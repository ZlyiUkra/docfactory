# Invalid value returned by a getStaticPaths path.
# джерело: https://github.com/withastro/docs/blob/9d4bd688365a562ccf013310d8840f3271c232d9/src/pages/en/reference/errors/invalid-get-static-path-param.mdx
# отримано: 2026-09-21
# версія: 2.0

> **InvalidGetStaticPathParam**: Invalid params given to `getStaticPaths` path. Expected an `object`, got `PARAM_TYPE` (E03010)

## What went wrong?

The `params` property in `getStaticPaths`'s return value (an array of objects) should also be an object.

```astro
---
export async function getStaticPaths() {
	return [
		{ params: { slug: "blog" } },
		{ params: { slug: "about" } }
	];
}
---
```

**See Also:**
-  `getStaticPaths()`
-  `params`
