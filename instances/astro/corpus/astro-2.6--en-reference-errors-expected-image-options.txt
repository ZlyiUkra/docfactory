# Expected image options.
# джерело: https://github.com/withastro/docs/blob/ad666cb07cb3a95e34e4567a10b5f19b138feb8f/src/content/docs/en/reference/errors/expected-image-options.mdx
# отримано: 2026-09-21
# версія: 2.6

> **ExpectedImageOptions**: Expected getImage() parameter to be an object. Received `OPTIONS`. (E03028)

## What went wrong?

`getImage()`'s first parameter should be an object with the different properties to apply to your image.

```ts

const optimizedImage = await getImage({src: myImage, width: 300, height: 300});
```

In most cases, this error happens because parameters were passed directly instead of inside an object.

**See Also:**
-  Assets (Experimental)
