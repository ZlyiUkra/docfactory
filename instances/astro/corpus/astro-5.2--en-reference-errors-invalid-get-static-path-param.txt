# Invalid value returned by a getStaticPaths path.
# джерело: https://github.com/withastro/docs/blob/47c3580ec64958652eb218deb6e42ea5f40cb28d/src/content/docs/en/reference/errors/invalid-get-static-path-param.mdx
# отримано: 2026-09-21
# версія: 5.2

> **InvalidGetStaticPathParam**: Invalid params given to `getStaticPaths` path. Expected an `object`, got `PARAM_TYPE`

## What went wrong?

The `params` property in `getStaticPaths`'s return value (an array of objects) should also be an object.

```astro
---
export async function getStaticPaths() {
	return [
		{ params: { slug: "blog" } },
		{ params: { slug: "about" } }
	];
}
---
```

**See Also:**
-  `getStaticPaths()`
-  `params`
