# Invalid value returned by a route from getStaticPaths().
# джерело: https://github.com/withastro/docs/blob/c086eca2a82f89a3a8daf2f6510f9c0ad9f83e75/src/content/docs/en/reference/errors/invalid-get-static-path-param.mdx
# отримано: 2026-09-21
# версія: 7.0

> **InvalidGetStaticPathParam**: Invalid `params` value returned by a route from `getStaticPaths()`. Expected an `object`, got `PARAM_TYPE`.

## What went wrong?

The `params` property in `getStaticPaths()`'s return value (an array of objects) should also be an object.

```astro
---
export async function getStaticPaths() {
	return [
		{ params: { id: "blog" } },
		{ params: { id: "about" } }
	];
}
---
```

**See Also:**
-  `getStaticPaths()`
-  `params`
