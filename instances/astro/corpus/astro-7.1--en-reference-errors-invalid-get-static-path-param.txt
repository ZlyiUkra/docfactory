# Invalid value returned by a route from getStaticPaths().
# джерело: https://github.com/withastro/docs/blob/170d368beecf3618478f65301be0fa95554030c5/src/content/docs/en/reference/errors/invalid-get-static-path-param.mdx
# отримано: 2026-09-21
# версія: 7.1

> **InvalidGetStaticPathParam**: Invalid `params` value returned by a route from `getStaticPaths()`. Expected an `object`, got `PARAM_TYPE`.

## What went wrong?

The `params` property in `getStaticPaths()`'s return value (an array of objects) should also be an object.

```astro
---
export async function getStaticPaths() {
	return [
		{ params: { id: "blog" } },
		{ params: { id: "about" } }
	];
}
---
```

**See Also:**
-  `getStaticPaths()`
-  `params`
