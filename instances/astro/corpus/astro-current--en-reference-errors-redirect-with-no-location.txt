# A redirect must be given a location with the Location header.
# джерело: https://github.com/withastro/docs/blob/main/src/content/docs/en/reference/errors/redirect-with-no-location.mdx
# отримано: 2026-09-21
# версія: 7

> **RedirectWithNoLocation**: The redirect `Response` has no `Location` header. Use `Astro.redirect()` to create redirects, or add a `Location` header to the `Response`.

## What went wrong?

A redirect must be given a location with the `Location` header.

**See Also:**
-  `Astro.redirect()`
