# Frontend API: Email Addresses
# джерело: https://clerk.com/docs/reference/frontend-api
# отримано: 2026-09-25

Used to interact with the email addresses of the logged in user.

## GET /v1/me/email_addresses

Get Email Addresses

Retrieve all the email addresses associated with the current user.

Parameters:

- `_clerk_session_id` (query) `string`: The session_id associated with the requesting user.

Responses:

- `200`: Returns UserEmailAddresses array.
- `401`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful

## POST /v1/me/email_addresses

Create Email Address

Add an email address to the current user. The address then needs to be verified using the `prepare_verification` and `attempt_verification` endpoints.

Parameters:

- `_clerk_session_id` (query) `string`: The session_id associated with the requesting user.

Request body (application/x-www-form-urlencoded):

- `email_address` `string`, required: The email address to be added to the user.

Responses:

- `200`: Returns the response for Session wrapped Email object.
- `400`: Request was not successful
- `401`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful
- `422`: Request was not successful

## POST /v1/me/email_addresses/{email_id}/attempt_verification

Attempt Email Address Verification

Attempt to verify an email address that was previously created.

Parameters:

- `email_id` (path, required) `string`: The email_id.
- `_clerk_session_id` (query) `string`: The session_id associated with the requesting user.

Request body (application/x-www-form-urlencoded):

- `code` `string`, required: The code that was previously sent to the email address.

Responses:

- `200`: Returns the response for Session wrapped Email object.
- `400`: Request was not successful
- `401`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful
- `422`: Request was not successful

## POST /v1/me/email_addresses/{email_id}/prepare_verification

Prepare Email Address Verification

Depending on the given strategy, the API will prepare the verification for the email address.
In particular, * for `email_code`, the API will send a verification email to the address containing a code. * for `email_link`, the API will send a verification email to the address containing a link to the verification attempt endpoint. * for `enterprise_sso`, the API will send an external redirect URL to the browser containing a link to the SSO verification endpoint.

Parameters:

- `email_id` (path, required) `string`: The email_id.
- `_clerk_session_id` (query) `string`: The session_id associated with the requesting user.

Request body (application/x-www-form-urlencoded):

- `strategy` `string, one of email_code, email_link, enterprise_sso`, required: The strategy to be prepared for email verification.
- `redirect_url` `string`: Used with the `email_link` strategy.
- `action_complete_redirect_url` `string`: Used with `oauth_[provider]` and `saml` strategies.

Responses:

- `200`: Returns the response for Session wrapped Email object.
- `401`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful
- `422`: Request was not successful

## GET /v1/me/email_addresses/{email_id}

Get Email Address

Retrieve an email address by ID.

Parameters:

- `email_id` (path, required) `string`: The email_id.
- `_clerk_session_id` (query) `string`: The session_id associated with the requesting user.

Responses:

- `200`: Returns the response for Session wrapped Email object.
- `401`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful

## DELETE /v1/me/email_addresses/{email_id}

Delete email address

Delete an email address by ID.

Parameters:

- `email_id` (path, required) `string`: The email_id.
- `_clerk_session_id` (query) `string`: The session_id associated with the requesting user.

Responses:

- `200`: Returns a deleted generic object.
- `400`: Request was not successful
- `401`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful
