# Frontend API: Sign Ups
# джерело: https://clerk.com/docs/reference/frontend-api
# отримано: 2026-09-25

Used to sign up a new user in the current client.

## POST /v1/client/sign_ups

Create Sign-up

Creates or replaces the sign-up on the current Client object.

Parameters:

- `Origin` (header) `string`: The origin of the request

Request body (application/x-www-form-urlencoded):

- `transfer` `boolean`
- `password` `string`
- `first_name` `string`
- `last_name` `string`
- `username` `string`
- `email_address` `string`
- `phone_number` `string`
- `email_address_or_phone_number` `string`
- `unsafe_metadata` `string`
- `strategy` `string`: Strategy used to sign up. Can be one of `email_code`, `email_link`, `enterprise_sso`, `google_one_tap`, `oauth_[provider]`, `oauth_token_[provider]`, `phone_code`, `saml`, `ticket`, `web3_[provider]_signature`
- `action_complete_redirect_url` `string`
- `redirect_url` `string`
- `ticket` `string`
- `web3_wallet` `string`
- `token` `string`: The ID token from an OpenID Connect flow. Only used with `oauth_token_[provider]` and `google_one_tap` strategies.
- `code` `string`: The authorization or grant code for an OAuth exchange. Only used with `oauth_token_[provider]` strategies.
- `captcha_token` `string`
- `captcha_error` `string`
- `captcha_widget_type` `string`
- `legal_accepted` `boolean`: Has the value `true` if the user has accepted the legal requirements.
- `oidc_login_hint` `string`: Used with `oauth_[provider]`. The given value will be forwarded to the OIDC `login_hint` parameter of the generated redirect URL.
- `oidc_prompt` `string`: Used with `oauth_[provider]` or `enterprise_sso`. The given value will be forwarded to the OIDC `prompt` parameter of the generated redirect URL. When using shared credentials this value might be adjusted for security reasons.
- `proof_token` `string`: Proof token produced by the Clerk Protect SDK.

Responses:

- `200`: Returns the sign up object, as well as the updated session object.
- `307`: Redirect, no body.
- `400`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful
- `409`: Request was not successful
- `422`: Request was not successful

## GET /v1/client/sign_ups/{sign_up_id}

Get Sign-up

Returns the sign-up by ID. Must be associated with the current Client object.

Parameters:

- `sign_up_id` (path, required) `string`: The sign up unique identifier.

Responses:

- `200`: Returns the sign up object, as well as the updated session object.
- `400`: Request was not successful
- `401`: Request was not successful
- `404`: Request was not successful

## PATCH /v1/client/sign_ups/{sign_up_id}

Update Sign-up

Updates the sign-up object specified by ID, with the supplied parameters.

Parameters:

- `sign_up_id` (path, required) `string`: The sign up unique identifier.
- `Origin` (header) `string`: The origin of the request

Request body (application/x-www-form-urlencoded):

- `password` `string`
- `first_name` `string`
- `last_name` `string`
- `username` `string`
- `email_address` `string`
- `phone_number` `string`
- `email_address_or_phone_number` `string`
- `unsafe_metadata` `string`
- `strategy` `string`: Strategy used to sign up. Can be one of `email_code`, `email_link`, `enterprise_sso`, `google_one_tap`, `oauth_[provider]`, `oauth_token_[provider]`, `phone_code`, `saml`, `ticket`, `web3_[provider]_signature`
- `redirect_url` `string`
- `action_complete_redirect_url` `string`
- `ticket` `string`
- `web3_wallet` `string`
- `token` `string`: The ID token from an OpenID Connect flow. Only used with `oauth_token_[provider]` and `google_one_tap` strategies.
- `code` `string`: The authorization or grant code for an OAuth exchange. Only used with `oauth_token_[provider]` strategies.
- `legal_accepted` `boolean`: Has the value `true` if the user has accepted the legal requirements.
- `oidc_login_hint` `string`: Used with `oauth_[provider]`. The given value will be forwarded to the OIDC `login_hint` parameter of the generated redirect URL.
- `oidc_prompt` `string`: Used with `oauth_[provider]` or `enterprise_sso`. The given value will be forwarded to the OIDC `prompt` parameter of the generated redirect URL. When using shared credentials this value might be adjusted for security reasons.
- `proof_token` `string`: Proof token produced by the Clerk Protect SDK.

Responses:

- `200`: Returns the sign up object, as well as the updated session object.
- `307`: Redirect, no body.
- `400`: Request was not successful
- `401`: Request was not successful
- `403`: Request was not successful
- `404`: Request was not successful
- `422`: Request was not successful

## POST /v1/client/sign_ups/{sign_up_id}/prepare_verification

Prepare Sign-up Identification Verification

Prepares verification for the sign-up specified by `{id}`.

Depending on the given strategy, the API will prepare the verification for the current sign-up.
In particular,
* for `email_code`, the API will send a verification email to the email address currently load up in the sign-up
* for `phone_code`, the API will send a verification SMS to the phone number currently load up in the sign-up

Parameters:

- `sign_up_id` (path, required) `string`: The sign up unique identifier.
- `Origin` (header) `string`: The origin of the request

Request body (application/x-www-form-urlencoded):

- `strategy` `string`: The verification strategy - email_code - email_link - phone_code - web3_metamask_signature - web3_base_signature - web3_coinbase_wallet_signature - web3_okx_wallet_signature - saml - oauth
- `redirect_url` `string`
- `action_complete_redirect_url` `string`
- `oidc_login_hint` `string`: Used with `oauth_[provider]`. The given value will be forwarded to the OIDC `login_hint` parameter of the generated redirect URL.
- `oidc_prompt` `string`: Used with `oauth_[provider]` or `enterprise_sso`. The given value will be forwarded to the OIDC `prompt` parameter of the generated redirect URL. When using shared credentials this value might be adjusted for security reasons.
- `proof_token` `string`: Proof token produced by the Clerk Protect SDK.

Responses:

- `200`: Returns the sign up object, as well as the updated session object.
- `400`: Request was not successful
- `401`: Request was not successful
- `422`: Request was not successful
- `429`: Request was not successful

## POST /v1/client/sign_ups/{sign_up_id}/attempt_verification

Attempt Sign-up Identification Verification

Attempts to verify the identification that corresponds to the given strategy using the given verification code.

Parameters:

- `sign_up_id` (path, required) `string`: The sign up unique identifier.
- `Origin` (header) `string`: The origin of the request

Request body (application/x-www-form-urlencoded):

- `strategy` `string, one of email_code, email_link, phone_code, web3_metamask_signature, web3_base_signature, web3_coinbase_wallet_signature, web3_okx_wallet_signature, web3_solana_signature, google_one_tap`: The verification strategy
- `code` `string`: The verification code
- `signature` `string`: The verification web3 signature
- `token` `string`: The ID token from an OpenID Connect flow. Only used with `oauth_token_[provider]` and `google_one_tap` strategies.

Responses:

- `200`: Returns the sign up object, as well as the updated session object.
- `307`: Redirect, no body.
- `400`: Request was not successful
- `401`: Request was not successful
- `403`: Request was not successful
- `409`: Request was not successful
- `422`: Request was not successful
- `429`: Request was not successful
