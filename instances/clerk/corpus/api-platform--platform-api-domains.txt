# Platform API: Domains
# джерело: https://clerk.com/docs/reference/platform-api
# отримано: 2026-09-25

An object representing a domain associated with a Clerk application. In development `instances`, a domain is provided by Clerk. Production `instances` require additional configuration to be set up.

## PATCH /platform/applications/{applicationID}/domain

Update application domain

Update the production domain for an application.

Security: platform_api_access_token

Parameters:

- `applicationID` (path, required) `string`: Application ID.

Request body (application/json):

- `name` `string`, required: The domain name.
- `proxy_path` `string`: Optional proxy path for provider domains.

Responses:

- `200`: Application domain updated successfully.
- `400`: Request was not successful
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found
- `422`: Invalid request parameters

## GET /platform/applications/{applicationID}/domains

List application domains

List all domains for an application's production instance.

Security: platform_api_access_token

Parameters:

- `applicationID` (path, required) `string`: Application ID.

Responses:

- `200`: Application domains retrieved successfully.
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found

## POST /platform/applications/{applicationID}/domains

Create application domain

Create a provider domain for an application's production instance.

Security: platform_api_access_token

Parameters:

- `applicationID` (path, required) `string`: Application ID.

Request body (application/json):

- `name` `string`, required: The domain name.
- `proxy_path` `string`: Optional proxy path for provider domains.
- `domain_intent_id` `string`: Optional domain intent ID to complete into this domain.

Responses:

- `201`: Application domain created successfully.
- `400`: Request was not successful
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found
- `409`: Conflict
- `422`: Invalid request parameters

## GET /platform/applications/{applicationID}/domains/{domainIDOrName}

Get application domain

Get domain information for an application.

Security: platform_api_access_token

Parameters:

- `applicationID` (path, required) `string`: Application ID.
- `domainIDOrName` (path, required) `string`: Domain ID or domain name.

Responses:

- `200`: Application domain retrieved successfully.
- `400`: Request was not successful
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found

## DELETE /platform/applications/{applicationID}/domains/{domainIDOrName}

Delete application domain

Delete a provider domain from an application's production instance.

Only provider domains can be deleted. The primary domain cannot be deleted.

Security: platform_api_access_token

Parameters:

- `applicationID` (path, required) `string`: Application ID.
- `domainIDOrName` (path, required) `string`: Domain ID or domain name.

Responses:

- `200`: Application domain deleted successfully.
- `400`: Request was not successful
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found
- `422`: Invalid request parameters

## GET /platform/applications/{applicationID}/domains/{domainIDOrName}/status

Get application domain status

Get the status of a domain for an application.

Security: platform_api_access_token

Parameters:

- `applicationID` (path, required) `string`: Application ID.
- `domainIDOrName` (path, required) `string`: Domain ID or domain name.

Responses:

- `200`: Application domain status retrieved successfully.
- `400`: Request was not successful
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found

## POST /platform/applications/{applicationID}/domains/{domainIDOrName}/dns_check

Trigger DNS check

Triggers a DNS check for a domain and returns the current domain status.

This endpoint ensures that at most one DNS check job is in-flight at any time.
If a check is already running or was recently performed, a 409 Conflict is returned.

Use this endpoint to trigger a new DNS verification check after configuring DNS records.

Security: platform_api_access_token

Parameters:

- `applicationID` (path, required) `string`: Application ID.
- `domainIDOrName` (path, required) `string`: Domain ID or domain name.

Responses:

- `200`: DNS check triggered and domain status returned successfully.
- `400`: Request was not successful
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found
- `409`: Conflict

## GET /platform/domains

List domains

List production instance domains for applications in the authenticated workspace.
Results are sorted by domain creation time in descending order (most recent first).

Set `include_development` to include development instance domains.

Security: platform_api_access_token

Parameters:

- `name` (query) `string`: Filter domains whose name contains this substring (case-insensitive).
- `include_development` (query) `string, one of true`: When `true`, include development instance domains. Defaults to production only.
- `limit` (query) `integer`: Number of results to return per page (1-500, default 10).
- `starting_after` (query) `string`: Cursor for pagination. Provide the ID of the last domain from the previous page to get the next page of results.
- `ending_before` (query) `string`: Cursor for pagination. Provide the ID of the first domain from the previous page to get the previous page of results.

Responses:

- `200`: Domains retrieved successfully.
- `401`: Authentication invalid
- `403`: Authorization invalid
- `422`: Invalid request parameters

## GET /platform/domains/{domainIDOrName}

Get domain

Retrieve a single domain by ID or name for the authenticated workspace.

Security: platform_api_access_token

Parameters:

- `domainIDOrName` (path, required) `string`: Domain ID or domain name.

Responses:

- `200`: Domain retrieved successfully.
- `401`: Authentication invalid
- `403`: Authorization invalid
- `404`: Resource not found
