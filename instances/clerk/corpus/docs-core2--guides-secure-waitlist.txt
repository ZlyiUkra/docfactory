# Set up a waitlist in your app
# джерело: https://github.com/clerk/clerk-docs/blob/core-2/docs/guides/secure/waitlist.mdx
# отримано: 2026-09-25
# версія: core-2

SDK: nextjs, react, expo, react-router, astro, nuxt, vue, chrome-extension, tanstack-react-start

In **Waitlist** mode, users can register their interest in your app by joining a waitlist. This mode is ideal for apps in early development stages or those wanting to generate interest before launch. This guide shows you how to add a waitlist to your application.

## Enable Waitlist mode

  To enable **Waitlist** mode, follow these steps:

  1. In the Clerk Dashboard, navigate to the **Waitlist** page.
  1. Toggle on **Enable waitlist** and select **Save**.

## Add the `<Waitlist />` component to your app

  The `<Waitlist />` component provides a form where users can submit their details to join the waitlist and express their interest in your app. Once approved by admins, users will receive an email with access instructions.

  SDK: nextjs

    > [!NOTE]
    > If you're using Next.js, the `<Waitlist />` component is available in `@clerk/nextjs@6.2.0` and above.

  The following example includes a basic implementation of the `<Waitlist />` component hosted under the `/waitlist` route. You can use this as a starting point for your own implementation.

  SDK: nextjs

```tsx
    import { Waitlist } from '@clerk/nextjs'

    export default function WaitlistPage() {
      return <Waitlist />
    }
```

  SDK: react

```tsx
    import { Waitlist } from '@clerk/clerk-react'

    export default function WaitlistPage() {
      return <Waitlist />
    }
```

  SDK: astro

```astro
    ---
    import { Waitlist as WaitlistAstro } from '@clerk/astro/components'
    ---

    <WaitlistAstro />
```

  SDK: expo

    > [!NOTE]
    > This component can be used in Expo Web projects, but it isn't available in native environments (iOS or Android). For native apps, build a custom UI using Clerk hooks. See custom flows guides for details.

```jsx
    import { Waitlist } from '@clerk/clerk-expo/web'

    export default function WaitlistPage() {
      return <Waitlist />
    }
```

  SDK: chrome-extension

```jsx
    import { Waitlist } from '@clerk/chrome-extension'

    export default function WaitlistPage() {
      return <Waitlist />
    }
```

  SDK: react-router

```tsx
    import { Waitlist } from '@clerk/react-router'

    export default function WaitlistPage() {
      return <Waitlist />
    }
```

  SDK: remix

```tsx
    import { Waitlist } from '@clerk/remix'

    export default function WaitlistPage() {
      return <Waitlist />
    }
```

  SDK: tanstack-react-start

```tsx
    import { Waitlist } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/waitlist')({
      component: WaitlistPage,
    })

    function WaitlistPage() {
      return <Waitlist />
    }
```

  SDK: nuxt

```vue
    <script setup>
    // Components are automatically imported
    </script>

    <template>
      <Waitlist />
    </template>
```

  SDK: vue

```vue
    <script setup lang="ts">
    import { Waitlist } from '@clerk/vue'
    </script>

    <template>
      <Waitlist />
    </template>
```

## Provide the `waitlistUrl` prop

  The `waitlistUrl` prop is used to specify the URL of your waitlist page. It should point to the route where your `<Waitlist />` component is mounted. If `undefined`, the user will be redirected to the Account Portal waitlist page. You'll need to set `waitlistUrl` where you initialize Clerk.

  SDK, except: astro, vue, nuxt

    Pass the `waitlistUrl` prop to the `<ClerkProvider>` component, ensuring it matches your waitlist route.

```tsx
    <ClerkProvider waitlistUrl="/waitlist">
      {/* rest of your layout */}
    </ClerkProvider>
```

  SDK: astro

    Pass the `waitlistUrl` prop to the `clerk()` integration, ensuring it matches your waitlist route.

```js
    import clerk from '@clerk/astro'

    export default defineConfig({
      integrations: [
        clerk({
          waitlistUrl: '/waitlist',
        }),
      ],
    })
```

  SDK: vue

    Pass the `waitlistUrl` prop to the `clerkPlugin()` integration, ensuring it matches your waitlist route.

```ts
    import { createApp } from 'vue'
    import App from './App.vue'
    import { clerkPlugin } from '@clerk/vue'

    const app = createApp(App)

    app.use(clerkPlugin, {
      waitlistUrl: '/waitlist',
    })

    app.mount('#app')
```

  SDK: nuxt

    Pass the `waitlistUrl` prop to the `defineNuxtConfig()` integration, ensuring it matches your waitlist route.

```ts
    export default defineNuxtConfig({
      modules: ['@clerk/nuxt'],
      clerk: {
        waitlistUrl: '/waitlist',
      },
    })
```

## Add a sign-in page

  To allow users to sign in once they've been approved from the waitlist, you need to set up a custom sign-in page.

  The following example demonstrates how to render the `<SignIn />` component, which will only be accessible to users who have been approved from the waitlist.

  SDK: nextjs

    If you would like to create a dedicated `/sign-in` page in your Next.js application, see the dedicated guide for more information.

```tsx
    import { SignIn } from '@clerk/nextjs'

    export default function SignInPage() {
      return <SignIn />
    }
```

  SDK: astro

```astro
    ---
    import { SignIn } from '@clerk/astro/components'
    ---

    <SignIn />
```

  SDK: expo

    > [!NOTE]
    > This component can be used in Expo Web projects, but it isn't available in native environments (iOS or Android). For native apps, build a custom UI using Clerk hooks. See custom flows guides for details.

    If you would like to create a dedicated `/sign-in` page in your Expo application, see the dedicated guide for more information.

```tsx
    import { SignIn } from '@clerk/clerk-expo/web'

    export default function SignInPage() {
      return <SignIn />
    }
```

  SDK: react

```jsx
    import { SignIn } from '@clerk/clerk-react'

    export default function SignInPage() {
      return <SignIn />
    }
```

  SDK: react-router

    If you would like to create a dedicated `/sign-in` page in your React Router application, see the dedicated guide for more information.

```tsx
    import { SignIn } from '@clerk/react-router'

    export default function SignInPage() {
      return <SignIn />
    }
```

  SDK: chrome-extension

```jsx
    import { SignIn } from '@clerk/chrome-extension'

    export default function SignInPage() {
      return <SignIn />
    }
```

  SDK: tanstack-react-start

    If you would like to create a dedicated `/sign-in` page in your TanStack React Start application, see the dedicated guide for more information.

```tsx
    import { SignIn } from '@clerk/tanstack-react-start'
    import { createFileRoute } from '@tanstack/react-router'

    export const Route = createFileRoute('/sign-in')({
      component: SignInPage,
    })

    function SignInPage() {
      return <SignIn />
    }
```

  SDK: vue

```vue
    <script setup lang="ts">
    import { SignIn } from '@clerk/vue'
    </script>

    <template>
      <SignIn />
    </template>
```

  SDK: nuxt

```vue
    <script setup lang="ts">
    // Components are automatically imported
    </script>

    <template>
      <SignIn />
    </template>
```

  > [!NOTE]
  > Alternatively, you can add the `waitlistUrl` prop to the `<SignIn />` component instead of `<ClerkProvider>` if needed.

## Manage users on your waitlist

  Once users join your waitlist, you can manage their access from the Clerk Dashboard. You can approve or deny users, which will trigger an email notification to them with instructions on how to access your app if approved.

  1. In the Clerk Dashboard, navigate to the **Waitlist** page.
  1. On the right-side of a user's row, select the menu icon (...).
  1. Select **Invite** to invite the user to your application. Select **Deny** to deny the user access to your application.
