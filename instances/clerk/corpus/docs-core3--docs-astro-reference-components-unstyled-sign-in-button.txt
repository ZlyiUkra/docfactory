# <SignInButton> component
# джерело: https://clerk.com/docs/astro/reference/components/unstyled/sign-in-button
# отримано: 2026-09-26
# версія: core-3

The `<SignInButton>` component is a button that, by default, links to your app's sign-in page. Your sign-in page will be hosted by Clerk using the Account Portal unless you have set up a dedicated sign-in page.

## Usage

### Basic usage

filename: pages/sign-in.astro
```astro
---
import { SignInButton } from '@clerk/astro/components'
---

<SignInButton />
```

### Custom usage

You can create a custom button by wrapping your own button, or button text, in the `<SignInButton>` component.

You must pass the `asChild` prop to the `<SignInButton>` component if you are passing children to it.

filename: pages/index.astro
```astro
---
import { SignInButton } from '@clerk/astro/components'
---

<SignInButton asChild>
  <button>Custom sign in button</button>
</SignInButton>
```

If you want to render multiple elements, wrap them in a single parent element:

```astro
<SignInButton asChild>
  <button>
    <span>Continue</span>
    <span aria-hidden="true"> →</span>
  </button>
</SignInButton>
```

## Properties

| Name                                                            | Type                          | Description                                                                                                                                                                                                                        |
| --------------------------------------------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| children?                                                       | React.ReactNode               | Children you want to wrap the  in. Only accepts one child; if you want to render multiple elements, wrap them in a single parent element.                                                                           |
| fallbackRedirectUrl?                                            | string                        | The fallback URL to redirect to after the user signs in, if there's no redirect\_url in the path already. Defaults to /. It's recommended to use the environment variable instead.                                                 |
| forceRedirectUrl?                                               | string                        | If provided, this URL will always be redirected to after the user signs in. It's recommended to use the environment variable instead.                                                                                              |
| initialValues                                                   | SignInInitialValues           | The values used to prefill the sign-in fields with.                                                                                                                                                                                |
| mode?                                                           | 'redirect' | 'modal'         | Determines what happens when a user clicks on the . Setting this to 'redirect' will redirect the user to the sign-in route. Setting this to 'modal' will open a modal on the current route. Defaults to 'redirect'. |
| "redirect": Redirect to the OAuth provider on the current page. | "popup": Open a popup window. | Defaults to "auto".                                                                                                                                                                                                                |
| signUpFallbackRedirectUrl?                                      | string                        | The fallback URL to redirect to after the user signs up, if there's no redirect\_url in the path already. Defaults to /. It's recommended to use the environment variable instead.                                                 |
| signUpForceRedirectUrl?                                         | string                        | If provided, this URL will always be redirected to after the user signs up. It's recommended to use the environment variable instead.                                                                                              |

| Name     | Type    | Description                                                                                  |
| -------- | ------- | -------------------------------------------------------------------------------------------- |
| asChild? | boolean | If true, the  component will render its children as a child of the component. |
