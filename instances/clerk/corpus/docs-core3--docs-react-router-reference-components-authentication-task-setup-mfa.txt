# <TaskSetupMFA /> component
# джерело: https://clerk.com/docs/react-router/reference/components/authentication/task-setup-mfa
# отримано: 2026-09-25
# версія: core-3

The  component renders a UI for resolving the setup-mfa session task.

The `<TaskSetupMFA />` component renders a UI for resolving the `setup-mfa` session task. You can further customize your `<TaskSetupMFA />` component by passing additional properties at the time of rendering.

> The `<TaskSetupMFA/>` component cannot render when a user doesn't have current session tasks.

## When to use `<TaskSetupMFA />`

Clerk's sign-in flows, such as the Sign-in Account Portal page, , and  component, automatically handle the `setup-mfa` session task flow for you, including rendering the `<TaskSetupMFA />` component when needed.

If you want to customize the route where the `<TaskSetupMFA />` component is rendered or customize its appearance, you can host it yourself within your application.

## Example

The following example demonstrates how to host the `<TaskSetupMFA />` component on a custom page. You first need to set the `taskUrls` option on your Clerk integration so that users are redirected to the page where you host the `<TaskSetupMFA />` component when they have a pending `setup-mfa` session task.

> To see the full `root.tsx` setup you need for Clerk with React Router, see the React Router quickstart.

filename: app/root.tsx
```tsx
import { ClerkProvider } from '@clerk/react-router'
import { Outlet } from 'react-router'

export default function App() {
  return (
    <ClerkProvider taskUrls={{ 'setup-mfa': '/session-tasks/setup-mfa' }}>
      <Outlet />
    </ClerkProvider>
  )
}
```

filename: app/routes/session-tasks/setup-mfa.tsx
```tsx
import { TaskSetupMFA } from '@clerk/react-router'

export default function SetupMfaPage() {
  return <TaskSetupMFA redirectUrlComplete="/dashboard" />
}
```

## Properties

| Name                | Type                    | Description                                                                                         |
| ------------------- | ----------------------- | --------------------------------------------------------------------------------------------------- |
| appearance?         | Appearance | undefined | An object to style your components. Will only affect Clerk components and not Account Portal pages. |
| redirectUrlComplete | string                  | The full URL or path to navigate to after successfully completing the task.                         |

## Customization

To learn about how to customize Clerk components, see the customization documentation.

If Clerk's prebuilt components don't meet your specific needs or if you require more control over the logic, you can rebuild the existing Clerk flows using the Clerk API. For more information, see the custom flow guides.

---

## Sitemap

Overview of all docs pages
