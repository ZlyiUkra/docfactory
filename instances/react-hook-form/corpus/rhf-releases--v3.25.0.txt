# React Hook Form release: Version 3.25.0
# джерело: https://github.com/react-hook-form/react-hook-form/releases/tag/v3.25.0
# отримано: 2026-09-26
# версія: 3.25.0

Tag v3.25.0, published 2019-10-24.

**Important:**
🐞 Fix `touched` to the only trigger during `onBlur` event
😥 I sincerely apologize to all the users, this is my bad understanding of `touched`. This bug was introduced on day 1 when react-hook-form introduced `formState`.
🐞 Fix dependencies during `useEffect` with `register` and `unregister` #394
🐞 Fix `isValid` issue with `validationFields` #389
🐞 Consistency with `isValid` under `onSubmit` `mode` always return `false` on form mount #389
🥂 Improve typescript support for react-native custom `register` #381
🏃‍♂️ Abort early for `validate` function (consistency)  #400
