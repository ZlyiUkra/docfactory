# React Hook Form release: Version 5.0.0
# джерело: https://github.com/react-hook-form/react-hook-form/releases/tag/v5.0.0
# отримано: 2026-09-26
# версія: 5.0.0

Tag v5.0.0, published 2020-03-06.

**Breaking change on Controller**

Controller: `onChange` will only evaluate payload as `event` like `object`. eg: react-select will no longer need the extra `onChange` method at `Controller`.

Before:
```
import { TextInput } from 'react-native';

<Controller
  as={<TextInput style={{ borderWidth: 2, borderColor: 'black'}} />}
  name="text"
  control={args => ({
    value: args[0].nativeEvent.text,
  })}
  onChange={onChange}
/>
```

now:
```
import { TextInput } from 'react-native';

<Controller
  as={<TextInput style={{ borderWidth: 2, borderColor: 'black'}} />}
  name="text"
  control={args => args[0].nativeEvent.text}
  onChange={onChange}
/>
```
