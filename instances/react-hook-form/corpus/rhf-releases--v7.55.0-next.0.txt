# React Hook Form release: 🎄 Version7.55.0-next.0
# джерело: https://github.com/react-hook-form/react-hook-form/releases/tag/v7.55.0-next.0
# отримано: 2026-09-26
# версія: 7.55.0-next.0

Tag v7.55.0-next.0, published 2024-12-24, pre-release.

⚡️ `createFormControl`

- Allow us to start subscribing outside of the react component

```tsx
const { formControl, control } = createFormControl(props)

function App() {
  const { register } = useForm({
    formControl,
  })

  return <form />
}

function Test() {
  useFormState({
    control // no longer need context api
  })
}
```

⚡️ `subscribe`

- subscribe form state update without re-render
- subscribe outside of the react component

```tsx
const { formControl } = createFormControl(props)

formControl.subscribe({
  formState: { isDirty: true },
  callback: (formState) => {
    if (formState.isDirty) {
      // do something here
    }
  }
})

function App() {
  const { register } = useForm({
    formControl,
  })

  return <form />
}
```
