# React Hook Form release: Version 7.57.0-next.0
# джерело: https://github.com/react-hook-form/react-hook-form/releases/tag/v7.57.0-next.0
# отримано: 2026-09-26
# версія: 7.57.0-next.0

Tag v7.57.0-next.0, published 2025-04-27, pre-release.

### 👀 `useWatch` new `compute` props

- subscribe to the entire form but only return updated value with certain condition

```tsx
type FormValue = {
  test: string;
}

const watchedValue = useWatch({
  control: methods.control,
  compute: (data: FormValue) => {
    if (data.test?.length) {
      return data.test;
    }

    return '';
  },
});
```

- subscribe to a specific form value state

```tsx
type FormValue = {
  test: string;
}

const watchedValue = useWatch({
  control: methods.control,
  name: 'test',
  compute: (data: string) => {
      return data.length > 3 ? data : '';
  },
});
```
