# React Hook Form release: Version 7.9.0
# джерело: https://github.com/react-hook-form/react-hook-form/releases/tag/v7.9.0
# отримано: 2026-09-26
# версія: 7.9.0

Tag v7.9.0, published 2021-06-19.

🥇 feature: support browser native form control (#5503)

```tsx
export default function App() {
  const { register, handleSubmit } = useForm({
    shouldUseNativeValidation: true,
  });
  const onSubmit = (data) => console.log(data);

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <input
        {...register("firstName", {
          required: "This is required from bill."
        })}
        autoComplete={"off"}
        placeholder="First Name"
      />
      <input type="submit" />
    </form>
  );
}
```

🐞 fix #5661 trigger throw error when the field is not found (#5664)
🐞 fix #5678 reset with keep default values with Controller (#5688)
