# components/ApiPage
# джерело: https://github.com/react-hook-form/documentation/blob/e771c023cce3d2a3e5681373462e9e4abdd82985/src/components/ApiPage.tsx
# отримано: 2026-09-26
# версія: 4.9.8-beta.2, 4.9.8-beta.1, 4.9.7, 4.9.6

## state

## API

API

## useForm: Function

`register` , `unregister` , `errors` , `watch` , `handleSubmit` , `reset` , `setError` , `clearError` , `setValue` , `getValues` , `triggerValidation` , `control` and `formState` .

##### mode: string = 'onSubmit'

onSubmit (Default) | string

onBlur | string

onChange | string

##### defaultValues: Record<string, any> = {}

```
const { register } = useForm({
  defaultValues: {
    firstName: "bill",
    lastName: "luo",
    email: "bluebill1049@hotmail.com",
    pets: [ 'dog', 'cat' ]
  }
})

<input name="firstName" ref={register} /> // ✅ working version
<input name="lastName" ref={() => register({ name: 'lastName' })} />
// ❌ above example does not work with "defaultValues" due to its "ref" not being provided
```

##### validationSchema: Object

##### validationResolver: Function

##### validationContext: Object

##### validateCriteriaMode: firstErrorDetected | all

##### reValidateMode: onChange | onBlur | onSubmit

##### submitFocusError: boolean = true

♦

## register: (Ref, validateRule?) => void

## unregister: (name: string | string[]) => void

## handleSubmit: (data: Object, e: Event) => void

## reset: (values?: Record<string, any>) => void

## setError: (name: string | ManualFieldError[], type?: string | Object, message?: string) => void

## clearError: (name?: string | string[]) => void

## setValue: (name: string, value: any, shouldValidate?: boolean) => void

## getValues: (payload?: { nest: boolean }) => Object

## triggerValidation: (payload?: string | string[]) => Promise

## control: Object

## validationSchema: Object

## Browser built-in validation (V3 only)

Note: This feature has been removed in V4 due to low usage, but you can still use it in V3
