# components/codeExamples/accessibleCodeFinal
# джерело: https://github.com/react-hook-form/documentation/blob/3646b2c2c73f4d04882b415205818635d075279e/src/components/codeExamples/accessibleCodeFinal.ts
# отримано: 2026-09-26
# версія: 6.0.0-rc.5, 6.0.0-rc.4, 6.0.0-rc.3, 6.0.0-rc.2, 6.0.0-rc.1, 6.0.0-rc.0, 5.7.2, 5.7.1, 5.7.0, 5.6.3, 5.6.2, 6.0.0-beta.2, 5.6.1, 5.6.1-beta.2, 5.6.1-beta.1, 5.6.0

```
import React from "react";
import { useForm } from "react-hook-form";

export default function App() {
  const { register, handleSubmit, errors } = useForm();
  const onSubmit = data => console.log(data);

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <label for="name">Name</label>
      <input
        type="text"
        id="name"
        {/* use aria-invalid to indicate field contain error */}
        aria-invalid={errors.name ? "true" : "false"}
        {/* use aria-describedby to associate with error messages */}
        aria-describedby="error-name-required error-name-maxLength"
        ref={register({ required: true, maxLength: 30 })}
      />
      {/* the id field is used to associated with aria-describedby*/}
      <span
        role="alert"
        id="error-name-required"
        style={{
          display: errors.name && errors.name.type === "required"
            ? "block"
            : "none"
        }}
      >
        This is required
      </span>
      <span
        role="alert"
        id="error-name-maxLength"
        style={{
          display: errors.name && errors.name.type === "maxLength"
            ? "block"
            : "none"
        }}
      >
        Max length exceeded
      </span>
      <input type="submit" />
    </form>
  );
}
```
