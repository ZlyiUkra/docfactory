# components/v6/ApiRefTableV6
# джерело: https://github.com/react-hook-form/documentation/blob/695829b109c5fd74bb8ced2e923516002ccac1e6/src/components/v6/ApiRefTableV6.tsx
# отримано: 2026-09-26
# версія: 6.15.6, 6.15.5

Input

`name="firstName"` | `{ firstName: 'value'}`

`name="name.firstName"` | `{ name: { firstName: 'value' } }`

`name="name.firstName[0]"` | `{ name: { firstName: [ 'value' ] } }`

`ref` `React.RefObject` | React element ref

`required` ``` string | { value: boolean, message: string | React.ReactElement } ``` | ``` <input name="test" ref={ register({ ${ isStandard ? "required: true" : `required: 'error message' // JS only: <p>error message</p> TS only support string` } }) } /> ```

`maxLength` ``` { value: number, message: string | React.ReactElement } ``` | ``` <input name="test" ref={ register({ ${ isStandard ? "maxLength: 2" : `maxLength : { value: 2, message: 'error message' // JS only: <p>error message</p> TS only support string }` } }) } /> ```

`minLength` ``` { value: number, message: string | React.ReactElement } ``` | ``` <input name="test" ref={ register({ minLength: ${ isStandard ? 1 : `{ value: 1, message: 'error message' // JS only: <p>error message</p> TS only support string }` } }) } /> ```

`max` ``` { value: number, message: string | React.ReactElement } ``` | ``` <input name="test" type="number" ref={ register({ max: ${ isStandard ? 3 : `{ value: 3, message: 'error message' // JS only: <p>error message</p> TS only support string }` } }) } /> ```

`min` ``` { value: number, message: string | React.ReactElement } ``` | ``` <input name="test" type="number" ref={ register({ min: ${ isStandard ? 3 : `{ value: 3, message: 'error message' // JS only: <p>error message</p> TS only support string }` } }) } /> ```

`pattern` ``` { value: RegExp, message: string | React.ReactElement } ``` | ``` <input name="test" ref={ register({ pattern: ${ isStandard ? "/[A-Za-z]{3}/" : `{ value: /[A-Za-z]{3}/, message: 'error message' // JS only: <p>error message</p> TS only support string }` } }) } /> ```

`validate` `Function | Object` | ``` <input name="test" ref={ register({ validate: ${ isStandard ? `value => value === '1'` : `value => value === '1' || 'error message' // JS only: <p>error message</p> TS only support string` } }) } /> // object of callback functions <input name="test1" ref={ register({ validate: ${ isStandard ? `{ positive: value => parseInt(value) > 0, lessThanTen: value => parseInt(value) < 10, asyncValidate: async () => await fetch(url) }` : `{ positive: value => parseInt(value) > 0 || 'should be greater than 0', lessThanTen: value => parseInt(value) < 10 || 'should be lower than 10', // you can do asynchronous validation as well asyncValidate: async () => await fetch(url) || 'error message' // JS only: <p>error message</p> TS only support string }` } }) } /> ```

valueAsNumber: `boolean` | Returns a Number normally. If something goes wrong `NaN` will be returned. Note: `valueAs` process is happening after validation. | ``` <input name="test" type="number" ref={ register({ valueAsNumber: true, }) } /> ```

valueAsDate: `boolean` | Returns a Date normally. If something goes wrong `null` will be returned. Note: `valueAs` process is happening after validation. | ``` <input name="test" type="date" ref={ register({ valueAsDate: true, }) } /> ```

setValueAs: `<T>(value: any) => T` | Return input value by running through the function. Note: `valueAs` process is happening after validation. Also, `setValueAs` is ignored if either `valueAsNumber` or `valueAsDate` are `true`. | ``` <input name="test" ref={ register({ setValueAs: (value) => parseInt(value), }) } /> ```
