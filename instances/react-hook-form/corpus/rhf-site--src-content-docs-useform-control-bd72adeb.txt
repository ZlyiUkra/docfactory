# control (content/docs/useform/control)
# джерело: https://github.com/react-hook-form/documentation/blob/aa307e47c7e16d3ddf5c71ccc23c00d1a07de35e/src/content/docs/useform/control.mdx
# отримано: 2026-09-26
# версія: 7.61.1, 7.61.0, 7.60.0, 7.60.0-next.0, 7.59.0, 7.58.1, 7.58.0, 7.57.0

## \</> `control:` Object

This object contains methods for registering components into React Hook Form.

Rules

**Important:** do not access any of the properties inside this object directly. It's for internal usage only.

**Examples:**

---

```tsx



type FormInputs = {
  firstName: string
}

function App() {
  const { control, handleSubmit } = useForm<FormInputs>()
  const onSubmit = (data: FormInputs) => console.log(data)

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <Controller
        as={TextField}
        name="firstName"
        control={control}
        defaultValue=""
      />

      <input type="submit" />
    </form>
  )
}
```

```javascript


function App() {
  const { control } = useForm()

  return (
    <Controller
      render={({ field }) => <input {...field} />}
      name="firstName"
      control={control}
      defaultValue=""
    />
  )
}
```
