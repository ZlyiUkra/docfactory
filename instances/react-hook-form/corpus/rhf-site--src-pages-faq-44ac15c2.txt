# pages/faq
# джерело: https://github.com/react-hook-form/documentation/blob/010786fc0a35d58c5927d17e8c695d49ebd7140d/src/pages/faq.tsx
# отримано: 2026-09-26
# версія: 3.21.19-beta.1, 3.21.18, 3.21.17, 3.21.16-ie11, 3.21.16

```
import React from "react";
import useForm from "react-hook-form";

function App() {
  const { register, handleSubmit } = useForm({
    defaultValues: {
      firstName: "bill",
      lastName: "luo",
      email: "bluebill1049@hotmail.com"
    }
  });
  const onSubmit = data => alert(JSON.stringify(data));

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <input name="firstName" ref={register} />
      <input name="lastName" ref={register} />
      <input name="email" ref={register} />

      <button type="submit">Submit</button>
    </form>
  );
}
```

```
import React, { useRef } from "react"
import useForm from "react-hook-form"

function App() {
  const { register, handleSubmit } = useForm()
  const firstNameRef = useRef()
  const onSubmit = data => alert(JSON.stringify(data))

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <input name="firstName" ref={(e) => {
        register(e)
        firstNameRef.current = e // you can still assign to your own ref
      }} />
      <input name="lastName" ref={(e) => {
        // register's first argument is ref, and second is validation rules
        register(e, { required: true })
      }} />

      <button>Submit</button>
    </form>
  );
}
```

Performance of React Hook Form

How to creat accessible input error and message?

Does it work with Class Component?

How to reset the form?

How to initialize form values?

How to share ref usage?

What if you don't have access to ref?

Why first keystroke is not working?

FAQ

## FAQs

## frequently asked question.

## Performance of React Hook Form

Performance is one of the primary goals for building this custom hook. React Hook Form relies on uncontrolled component, hence the reason why the register function occurred at the ref. This approach will reduce the amount of re-rendering which occurred by user typing or value changing. Components mount to the page is much quicker too because they are not controlled. For mounting speed, I have completed a quick comparison test which you can refer to by this repo link .

## How to creat accessible input error and message?

React hook form is based on Uncontrolled Component , which give you the ability to building accessible custom form easily.

## Does it work with Class Component?

No, not out of box. but you can build a wrapper around it and use in your Class Component.

You can’t use Hooks inside of a class component, but you can definitely mix classes and function components with Hooks in a single tree. Whether a component is a class or a function that uses Hooks is an implementation detail of that component. In the longer term, we expect Hooks to be the primary way people write React components.

## How to reset the form?

There are two types of methods to clear form.

- HTMLFormElement.reset() This method does the same thing as clicking a form's reset button, and only clear `input/select/checkbox` value.

- React Hook Form API: `reset()` React Hook Form's `reset` method will reset all fields value, and also clear all `errors` within the form.

## How to initialize form values?

React Hook Form relies on uncontrolled component. With an uncontrolled component, you can specify a `defaultValue` or `defaultChecked` to individual field. However, the hook itself does provider an easier way to initialise all input values too. Example below:

## How to share ref usage?

React Hook Form need `ref` to collect input value, however you may want to use `ref` for other purpose (eg. scroll into the view). The following example will show you how.

## What if you don't have access to `ref`?

You can actually `register` an input without ref. In fact, you can manually `setValue`, `setError` and `triggerValidation`.

Note: because `ref` is not been registered, React Hook Form wouldn't be able to register event listeners to the inputs. so you will have to manually update value and error.

## Browser Support?

React Hook Form support all major browsers.

For legacy IE11 support, the following polyfills are required:

- `Array.prototype.includes`

- `Object.prototype.values`

- `Promise`

## Why first keystroke is not working?

Double check if you are using `value` instead of `defaultValue`.

React Hook Form is built based on uncontrolled input, which means you don't need to change input `value` via `state` by `onChange`. Hence you don't need `value` at all, in fact, you only need `defaultValue` for initial input value.
