# ApiRefTable
# джерело: https://github.com/react-hook-form/react-hook-form/blob/91c7a65fc5d5274cb5753bc98ab7b62bc1738115/website/src/ApiRefTable.tsx
# отримано: 2026-09-26
# версія: 2.1.16-beat.1, 2.1.15, 2.1.15-beta.4, 2.1.15-beta.3, 2.1.15-beta.2, 2.1.15-beta.1, 2.1.14, 2.1.13, 2.1.13-beta.4, 2.1.13-beta.3, 2.1.13-beta.2, 2.1.13-beta.1, 2.1.12, 2.1.12-beta.3, 2.1.12-beta.2, 2.1.12-beta.1, 2.1.11, 2.1.10, 2.1.10-beta.3, 2.1.10-beta.2, 2.1.10-beta.1, 2.1.9, 2.1.8, 2.1.7, 2.1.6, 2.1.5

This is the function to register `Ref` and validation into `react-hook-form`. Validation rules are all based on html input/select's standard validation, however `react-hook-form` do allow custom validation with method `validate`.

Important: input name is required and unique for `react-hook-form` in order to register the input.

If you working on arrays/array fields, you can assign input name as `name[index]`. Check out the example here .
Register options Standard register with validation Register with validation and error message

Name | Type | Description | Example

`ref` | `HTMLInputElement` | React element ref | ``` <input name="test" ref={register} /> ```

`required` | A Boolean which, if true, indicates that the input must have a value before the form can be submitted. you can assign as string to return error message in the `errors` object. | ``` <input name="test" ref={ register({ ${isStandard ? 'required: true' : `required: 'error message'`} }) } /> ```

`maxLength` | ``` { value: number, message: string } ``` | The maximum length of the value to accept for this input. | ``` <input name="test" ref={ register({ ${ isStandard ? 'maxLength: 2' : `maxLength : { value: 2, message: 'error message' }` } }) } /> ```

`minLength` | ``` { value: number, message: string } ``` | The minimum length of the value to accept for this input. | ``` <input name="test" ref={ register({ minLength: ${ isStandard ? 1 : `{ value: 1, message: 'error message' }` } }) } /> ```

`max` | ``` { value: number, message: string } ``` | The maximum value to accept for this input. | ``` <input name="test" ref={ register({ max: ${ isStandard ? 3 : `{ value: 3, message: 'error message' }` } }) } /> ```

`min` | ``` { value: number, message: string } ``` | The minimum value to accept for this input. | ``` <input name="test" ref={ register({ min: ${ isStandard ? 3 : `{ value: 3, message: 'error message' }` } }) } /> ```

`pattern` | ``` { value: RegExp, message: string } ``` | The regex pattern for the input. | ``` <input name="test" ref={ register({ pattern: ${ isStandard ? '\\[A-Za-z]{3}\\' : `{ value: \\[A-Za-z]{3}\\, message: 'error message' }` } }) } /> ```

`validate` | ``` { value: (Object) => Boolean | { [key: string]: (Object) => Boolean}, message: string } ``` | You can pass a callback function with value as the argument to validate, or you can pass an object of callback functions to validate all of them. (refer to the examples) | ``` <input name="single" ref={ register({ validate: ${ isStandard ? `(value) => value === '1'` : `(value) => { return value === '1' ? 'error message' : true; }` } }) } /> <input name="multiple" ref={ register({ validate: { isMoreThanOne: (value) => ${ isStandard ? 'value > 1' : `{ return value > 1 ? 'error message': true; }` }, isLessThanTen: (value) => ${ isStandard ? 'value > 1' : `{ return value < 10 ? 'error message': true; }` }, } }) } /> ```
