Loaded right now: {excerpts} excerpts from {documents} documents of the official React Hook
Form documentation -- the text of react-hook-form.com as it stood at every one of the npm
releases (from 0.0.12 in March 2019 to the current 7.x and the 8.0 betas, with every alpha,
beta, rc and next build), each distinct text once with all the versions it belongs to; the
README of every release; the TypeScript signatures of the public API (API Extractor report)
since December 2021; the CHANGELOG; the GitHub release notes; and the Resources section of
the site (articles, videos, newsletters, 3rd party bindings -- as annotated links, not the
articles themselves). Release lines present: {versions}. @hookform/resolvers, @hookform/
devtools, the Form Builder and the libraries RHF is used with (Zod, Yup, MUI) are not here;
if something about React Hook Form is not found in these documents, it is unlikely to be
anywhere in its official documentation.
