# react-hook-form · примірник фабрики: документація React Hook Form усіх версій. Лише те, що потрібно.
#
# Python 3.10+. Склад той самий, що в tanstack-query: жодне джерело не видається PDF-ом,
# а читачі репозиторію, реєстру npm і коду сайту (markdown, JSON, JSX) — на стандартній
# бібліотеці. Версії й верхні межі — ті самі, що в instances/react/requirements.txt.

anthropic>=0.40,<1
python-dotenv>=1.0.0

# MCP-сервер і клієнт. У SDK 2.0 клас FastMCP перейменували на MCPServer —
# код підтримує обидві версії.
mcp[cli]>=2.1,<3

# Пошук за змістом (необовʼязковий). ONNX замість torch: імпортується за секунди,
# без тисяч дрібних файлів. Без нього працює пошук лише по словах.
fastembed>=0.8,<1
