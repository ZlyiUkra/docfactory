# Function unstable_getRSCStream
# джерело: https://api.reactrouter.com/v7/functions/react-router.dom.unstable_getRSCStream.html
# отримано: 2026-09-25
# версія: 7.18.4

- unstable_getRSCStream(): ReadableStream

Get the prerendered RSC stream for hydration. Usually passed directly to your `react-server-dom-xyz/client`'s `createFromReadableStream`.

#### Returns ReadableStream

A `ReadableStream` that contains the RSC data for hydration.

#### Example

```
import { startTransition, StrictMode } from "react";
import { hydrateRoot } from "react-dom/client";
import {
  unstable_getRSCStream as getRSCStream,
  unstable_RSCHydratedRouter as RSCHydratedRouter,
} from "react-router";
import type { unstable_RSCPayload as RSCPayload } from "react-router";

createFromReadableStream(getRSCStream()).then(
  (payload: RSCServerPayload) => {
    startTransition(async () => {
      hydrateRoot(
        document,
        <StrictMode>
          <RSCHydratedRouter {...props} />
        </StrictMode>,
        {
          // Options
        }
      );
    });
  }
);
```

#### Name

unstable_getRSCStream

#### Mode

data

- Defined in packages/react-router/lib/rsc/html-stream/browser.ts:47
