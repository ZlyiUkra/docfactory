# Function unstable_RSCHydratedRouter
# джерело: https://api.reactrouter.com/v7/functions/react-router.dom.unstable_RSCHydratedRouter.html
# отримано: 2026-09-25
# версія: 7.18.4

- unstable_RSCHydratedRouter(props: unstable_RSCHydratedRouterProps): Element

Hydrates a server rendered unstable_RSCPayload in the browser.

#### Parameters

- props: unstable_RSCHydratedRouterProps

Props

Props for the unstable_RSCHydratedRouter component.

- ##### createFromReadableStream: unstable_BrowserCreateFromReadableStreamFunction

Your `react-server-dom-xyz/client`'s `createFromReadableStream` function, used to decode payloads from the server.

- ##### `Optional`fetch?: (request: Request) => Promise<Response>

Optional fetch implementation. Defaults to global `fetch`.

- ##### `Optional`getContext?: () => MaybePromise<RouterContextProvider>

A function that returns an RouterContextProvider instance which is provided as the `context` argument to client `action`s, `loader`s and middleware. This function is called to generate a fresh `context` instance on each navigation or fetcher call.

- ##### payload: unstable_RSCPayload

The decoded unstable_RSCPayload to hydrate.

#### Returns Element

A hydrated DataRouter that can be used to navigate and render routes.

#### Example

```
import { startTransition, StrictMode } from "react";
import { hydrateRoot } from "react-dom/client";
import {
  unstable_getRSCStream as getRSCStream,
  unstable_RSCHydratedRouter as RSCHydratedRouter,
} from "react-router";
import type { unstable_RSCPayload as RSCPayload } from "react-router";

createFromReadableStream(getRSCStream()).then((payload) =>
  startTransition(async () => {
    hydrateRoot(
      document,
      <StrictMode>
        <RSCHydratedRouter
          createFromReadableStream={createFromReadableStream}
          payload={payload}
        />
      </StrictMode>,
      { formState: await getFormState(payload) },
    );
  }),
);
```

#### Name

unstable_RSCHydratedRouter

#### Mode

data

- Defined in packages/react-router/lib/rsc/browser.tsx:717
