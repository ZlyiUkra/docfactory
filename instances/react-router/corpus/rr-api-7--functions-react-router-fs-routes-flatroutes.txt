# Function flatRoutes
# джерело: https://api.reactrouter.com/v7/functions/_react-router_fs-routes.flatRoutes.html
# отримано: 2026-09-25
# версія: 7.18.4

- flatRoutes(
options?: { ignoredRouteFiles?: string[]; rootDirectory?: string },
): Promise<RouteConfigEntry[]>

Creates route config from the file system using a convention that matches Remix v2's route file naming, for use within `routes.ts`.

#### Parameters

- options: { ignoredRouteFiles?: string[]; rootDirectory?: string } = {}

- ##### `Optional`ignoredRouteFiles?: string[]

An array of minimatch globs that match files to ignore. Defaults to `[]`.

- ##### `Optional`rootDirectory?: string

The directory containing file system routes, relative to the app directory. Defaults to `"./routes"`.

#### Returns Promise<RouteConfigEntry[]>

- Defined in index.ts:18
