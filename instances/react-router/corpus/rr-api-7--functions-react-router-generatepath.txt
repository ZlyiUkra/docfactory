# Function generatePath
# джерело: https://api.reactrouter.com/v7/functions/react-router.generatePath.html
# отримано: 2026-09-25
# версія: 7.18.4

- generatePath<Path extends string>(
originalPath: Path,
params?: {
[K in string | number | symbol]: (
ParseParams<Path> & { [key: string]: undefined | null | string }
)[K]
},
): string

Returns a path with params interpolated.

#### Type Parameters

- Path extends string

#### Parameters

- originalPath: Path

The original path to generate.

- params: {
[K in string | number | symbol]: (
ParseParams<Path> & { [key: string]: undefined | null | string }
)[K]
} = ...

The parameters to interpolate into the path.

#### Returns string

The generated path with parameters interpolated.

#### Example

```
import { generatePath } from "react-router";

generatePath("/users/:id", { id: "123" }); // "/users/123"

@public
```

- Defined in packages/react-router/lib/router/utils.ts:1449
