# Function Meta
# джерело: https://api.reactrouter.com/v7/functions/react-router.Meta.html
# отримано: 2026-09-25
# версія: 7.18.4

- Meta(): Element

Renders all the `<meta>` tags created by the route module's `meta` export. You should render it inside the `<head>` of your document.

#### Returns Element

A collection of React elements for `<meta>` tags

#### Example

```
import { Meta } from "react-router";

export default function Root() {
  return (
    <html>
      <head>
        <Meta />
      </head>
    </html>
  );
}

@public
```

#### Mode

framework

- Defined in packages/react-router/lib/dom/ssr/components.tsx:623
