# Function remixRoutesOptionAdapter
# джерело: https://api.reactrouter.com/v7/functions/_react-router_remix-routes-option-adapter.remixRoutesOptionAdapter.html
# отримано: 2026-09-25
# версія: 7.18.4

- remixRoutesOptionAdapter(
routes: (
defineRoutes: DefineRoutesFunction,
) => RouteManifest | Promise<RouteManifest>,
): Promise<RouteConfigEntry[]>

Adapts routes defined using Remix's `routes` config option to React Router's config format, for use within `routes.ts`.

#### Parameters

- routes: (defineRoutes: DefineRoutesFunction) => RouteManifest | Promise<RouteManifest>

#### Returns Promise<RouteConfigEntry[]>

- Defined in index.ts:17
