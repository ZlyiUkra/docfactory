# Function StaticRouterProvider
# джерело: https://api.reactrouter.com/v7/functions/react-router.StaticRouterProvider.html
# отримано: 2026-09-25
# версія: 7.18.4

- StaticRouterProvider(props: StaticRouterProviderProps): Element

A DataRouter that may not navigate to any other Location. This is useful on the server where there is no stateful UI.

#### Parameters

- props: StaticRouterProviderProps

Props

- ##### context: StaticHandlerContext

The StaticHandlerContext returned from StaticHandler's `query`

- ##### `Optional`hydrate?: boolean

Whether to hydrate the router on the client (default `true`)

- ##### `Optional`nonce?: string

The `nonce` to use for the hydration `<script>` tag

- ##### router: DataRouter

The static DataRouter from createStaticRouter

#### Returns Element

A React element that renders the static router provider

#### Example

```
export async function handleRequest(request: Request) {
  let { query, dataRoutes } = createStaticHandler(routes);
  let context = await query(request));

  if (context instanceof Response) {
    return context;
  }

  let router = createStaticRouter(dataRoutes, context);
  return new Response(
    ReactDOMServer.renderToString(<StaticRouterProvider ... />),
    { headers: { "Content-Type": "text/html" } }
  );
}

@public
```

#### Mode

data

- Defined in packages/react-router/lib/dom/server.tsx:162
