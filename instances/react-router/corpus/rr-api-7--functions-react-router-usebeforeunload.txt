# Function useBeforeUnload
# джерело: https://api.reactrouter.com/v7/functions/react-router.useBeforeUnload.html
# отримано: 2026-09-25
# версія: 7.18.4

- useBeforeUnload(
callback: (event: BeforeUnloadEvent) => any,
options?: { capture?: boolean },
): void

Set up a callback to be fired on Window's `beforeunload` event.

#### Parameters

- callback: (event: BeforeUnloadEvent) => any

The callback to be called when the `beforeunload` event is fired.

- `Optional`options: { capture?: boolean }

Options

- ##### `Optional`capture?: boolean

If `true`, the event will be captured during the capture phase. Defaults to `false`.

#### Returns void

- Defined in packages/react-router/lib/dom/lib.tsx:3233
