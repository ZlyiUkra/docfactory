# Function useNavigation
# джерело: https://api.reactrouter.com/v7/functions/react-router.useNavigation.html
# отримано: 2026-09-25
# версія: 7.18.4

- useNavigation(): UseNavigationResult

Returns the current Navigation, defaulting to an "idle" navigation when no navigation is in progress. You can use this to render pending UI (like a global spinner) or read `FormData` from a form navigation.

#### Returns UseNavigationResult

The current Navigation object

#### Example

```
import { useNavigation } from "react-router";

function SomeComponent() {
  let navigation = useNavigation();
  navigation.state;
  navigation.formData;
  // etc.
}

@public
```

#### Mode

framework

#### Mode

data

- Defined in packages/react-router/lib/hooks.tsx:1503
