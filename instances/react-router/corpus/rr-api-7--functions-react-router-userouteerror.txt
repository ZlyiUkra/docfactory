# Function useRouteError
# джерело: https://api.reactrouter.com/v7/functions/react-router.useRouteError.html
# отримано: 2026-09-25
# версія: 7.18.4

- useRouteError(): unknown

Accesses the error thrown during an `action`, `loader`, or component render to be used in a route module `ErrorBoundary`.

#### Returns unknown

The error that was thrown during route loading, `action` execution, or rendering

#### Example

```
export function ErrorBoundary() {
  const error = useRouteError();
  return <div>{error.message}</div>;
}

@public
```

#### Mode

framework

#### Mode

data

- Defined in packages/react-router/lib/hooks.tsx:1711
