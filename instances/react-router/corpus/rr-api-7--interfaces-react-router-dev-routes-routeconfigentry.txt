# Interface RouteConfigEntry
# джерело: https://api.reactrouter.com/v7/interfaces/_react-router_dev.routes.RouteConfigEntry.html
# отримано: 2026-09-25
# версія: 7.18.4

Configuration for an individual route, for use within `routes.ts`. As a convenience, route config entries can be created with the route, index and layout helper functions.

interface RouteConfigEntry {
caseSensitive?: boolean;
children?: RouteConfigEntry[];
file: string;
id?: string;
index?: boolean;
path?: string;
}

- Defined in config/routes.ts:68

##### Index

### Properties

caseSensitive? children? file id? index? path?

## Properties

### `Optional`caseSensitive

caseSensitive?: boolean

Should be `true` if the `path` is case-sensitive. Defaults to `false`.

- Defined in config/routes.ts:87

### `Optional`children

children?: RouteConfigEntry[]

The child routes.

- Defined in config/routes.ts:98

### file

file: string

The path to the entry point for this route, relative to `config.appDirectory`.

- Defined in config/routes.ts:93

### `Optional`id

id?: string

The unique id for this route.

- Defined in config/routes.ts:72

### `Optional`index

index?: boolean

Should be `true` if it is an index route. This disallows child routes.

- Defined in config/routes.ts:82

### `Optional`path

path?: string

The path this route uses to match on the URL pathname.

- Defined in config/routes.ts:77
