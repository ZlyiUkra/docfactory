# Interface SubmitOptions
# джерело: https://api.reactrouter.com/v7/interfaces/react-router.SubmitOptions.html
# отримано: 2026-09-25
# версія: 7.18.4

Submit options available to navigations

interface SubmitOptions {
action?: string;
defaultShouldRevalidate?: boolean;
encType?: FormEncType;
fetcherKey?: string;
flushSync?: boolean;
method?: HTMLFormMethod;
navigate?: boolean;
preventScrollReset?: boolean;
relative?: RelativeRoutingType;
replace?: boolean;
state?: any;
viewTransition?: boolean;
}

#### Hierarchy (View Summary)

- FetcherSubmitOptions

- SubmitOptions

- Defined in packages/react-router/lib/dom/dom.ts:221

##### Index

### Properties

action? defaultShouldRevalidate? encType? fetcherKey? flushSync? method? navigate? preventScrollReset? relative? replace? state? viewTransition?

## Properties

### `Optional`action

action?: string

The action URL path used to submit the form. Overrides `<form action>`. Defaults to the path of the current route.

Inherited from FetcherSubmitOptions.action

- Defined in packages/react-router/lib/dom/dom.ts:173

### `Optional`defaultShouldRevalidate

defaultShouldRevalidate?: boolean

Specify the default revalidation behavior after this submission

If no `shouldRevalidate` functions are present on the active routes, then this value will be used directly. Otherwise it will be passed into `shouldRevalidate` so the route can make the final determination on revalidation. This can be useful when updating search params and you don't want to trigger a revalidation.

By default (when not specified), loaders will revalidate according to the routers standard revalidation behavior.

Inherited from FetcherSubmitOptions.defaultShouldRevalidate

- Defined in packages/react-router/lib/dom/dom.ts:210

### `Optional`encType

encType?: FormEncType

The encoding used to submit the form. Overrides `<form encType>`. Defaults to "application/x-www-form-urlencoded".

Inherited from FetcherSubmitOptions.encType

- Defined in packages/react-router/lib/dom/dom.ts:179

### `Optional`fetcherKey

fetcherKey?: string

Indicate a specific fetcherKey to use when using navigate=false

- Defined in packages/react-router/lib/dom/dom.ts:237

### `Optional`flushSync

flushSync?: boolean

Enable flushSync for this submission's state updates

Inherited from FetcherSubmitOptions.flushSync

- Defined in packages/react-router/lib/dom/dom.ts:197

### `Optional`method

method?: HTMLFormMethod

The HTTP method used to submit the form. Overrides `<form method>`. Defaults to "GET".

Inherited from FetcherSubmitOptions.method

- Defined in packages/react-router/lib/dom/dom.ts:167

### `Optional`navigate

navigate?: boolean

navigate=false will use a fetcher instead of a navigation

- Defined in packages/react-router/lib/dom/dom.ts:242

### `Optional`preventScrollReset

preventScrollReset?: boolean

In browser-based environments, prevent resetting scroll after this navigation when using the component

Inherited from FetcherSubmitOptions.preventScrollReset

- Defined in packages/react-router/lib/dom/dom.ts:192

### `Optional`relative

relative?: RelativeRoutingType

Determines whether the form action is relative to the route hierarchy or the pathname. Use this if you want to opt out of navigating the route hierarchy and want to instead route based on /-delimited URL segments

Inherited from FetcherSubmitOptions.relative

- Defined in packages/react-router/lib/dom/dom.ts:186

### `Optional`replace

replace?: boolean

Set `true` to replace the current entry in the browser's history stack instead of creating a new one (i.e. stay on "the same page"). Defaults to `false`.

- Defined in packages/react-router/lib/dom/dom.ts:227

### `Optional`state

state?: any

State object to add to the history stack entry for this navigation

- Defined in packages/react-router/lib/dom/dom.ts:232

### `Optional`viewTransition

viewTransition?: boolean

Enable view transitions on this submission navigation

- Defined in packages/react-router/lib/dom/dom.ts:247
