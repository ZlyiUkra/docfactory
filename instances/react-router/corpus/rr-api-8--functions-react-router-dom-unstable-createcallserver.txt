# Function unstable_createCallServer
# джерело: https://api.reactrouter.com/v8/functions/react-router.dom.unstable_createCallServer.html
# отримано: 2026-09-25
# версія: 8.4.0

- unstable_createCallServer(
opts: {
createFromReadableStream: unstable_BrowserCreateFromReadableStreamFunction;
createTemporaryReferenceSet: () => unknown;
encodeReply: unstable_EncodeReplyFunction;
fetch?: (request: Request) => Promise<Response>;
},
): (id: string, args: unknown[]) => Promise<unknown>

Create a React `callServer` implementation for React Router.

#### Parameters

- opts: {
createFromReadableStream: unstable_BrowserCreateFromReadableStreamFunction;
createTemporaryReferenceSet: () => unknown;
encodeReply: unstable_EncodeReplyFunction;
fetch?: (request: Request) => Promise<Response>;
}

Options

- ##### createFromReadableStream: unstable_BrowserCreateFromReadableStreamFunction

Your `react-server-dom-xyz/client`'s `createFromReadableStream`. Used to decode payloads from the server.

- ##### createTemporaryReferenceSet: () => unknown

A function that creates a temporary reference set for the RSC payload.

- ##### encodeReply: unstable_EncodeReplyFunction

Your `react-server-dom-xyz/client`'s `encodeReply`. Used when sending payloads to the server.

- ##### `Optional`fetch?: (request: Request) => Promise<Response>

Optional Fetch API implementation. Defaults to global `fetch`.

#### Returns (id: string, args: unknown[]) => Promise<unknown>

A function that can be used to call server actions.

#### Example

```
import {
  createFromReadableStream,
  createTemporaryReferenceSet,
  encodeReply,
  setServerCallback,
} from "@vitejs/plugin-rsc/browser";
import { unstable_createCallServer as createCallServer } from "react-router";

setServerCallback(
  createCallServer({
    createFromReadableStream,
    createTemporaryReferenceSet,
    encodeReply,
  })
);
```

#### Name

unstable_createCallServer

#### Mode

data

- Defined in packages/react-router/lib/rsc/browser.tsx:112
