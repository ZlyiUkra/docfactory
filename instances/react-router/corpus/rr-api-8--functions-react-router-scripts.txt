# Function Scripts
# джерело: https://api.reactrouter.com/v8/functions/react-router.Scripts.html
# отримано: 2026-09-25
# версія: 8.4.0

- Scripts(scriptProps: ScriptsProps): Element | null

Renders the client runtime of your app. It should be rendered inside the `<body>` of the document.

If server rendering, you can omit `<Scripts/>` and the app will work as a traditional web app without JavaScript, relying solely on HTML and browser behaviors.

#### Parameters

- scriptProps: ScriptsProps

Additional props to spread onto the `<script>` tags, such as `crossOrigin`, `nonce`, etc.

#### Returns Element | null

A collection of React elements for `<script>` tags

#### Example

```
import { Scripts } from "react-router";

export default function Root() {
  return (
    <html>
      <head />
      <body>
        <Scripts />
      </body>
    </html>
  );
}

@public
```

#### Mode

framework

- Defined in packages/react-router/lib/dom/ssr/components.tsx:799
