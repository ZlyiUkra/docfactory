# Function useLoaderData
# джерело: https://api.reactrouter.com/v8/functions/react-router.useLoaderData.html
# отримано: 2026-09-25
# версія: 8.4.0

- useLoaderData<T = any>(): SerializeFrom<T>

Returns the data from the closest route `loader` or `clientLoader`.

#### Type Parameters

- T = any

#### Returns SerializeFrom<T>

The data returned from the route's `loader` or `clientLoader` function

#### Example

```
import { useLoaderData } from "react-router";

export async function loader() {
  return await fakeDb.invoices.findAll();
}

export default function Invoices() {
  let invoices = useLoaderData<typeof loader>();
  // ...
}

@public
```

#### Mode

framework

#### Mode

data

- Defined in packages/react-router/lib/hooks.tsx:1623
