# Function useLocation
# джерело: https://api.reactrouter.com/v8/functions/react-router.useLocation.html
# отримано: 2026-09-25
# версія: 8.4.0

- useLocation(): Location

Returns the current Location. This can be useful if you'd like to perform some side effect whenever it changes.

#### Returns Location

The current Location object

#### Example

```
import * as React from 'react'
import { useLocation } from 'react-router'

function SomeComponent() {
  let location = useLocation()

  React.useEffect(() => {
    // Google Analytics
    ga('send', 'pageview')
  }, [location]);

  return (
    // ...
  );
}

@public
```

- Defined in packages/react-router/lib/hooks.tsx:164
