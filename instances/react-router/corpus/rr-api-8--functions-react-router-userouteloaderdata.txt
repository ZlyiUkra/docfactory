# Function useRouteLoaderData
# джерело: https://api.reactrouter.com/v8/functions/react-router.useRouteLoaderData.html
# отримано: 2026-09-25
# версія: 8.4.0

- useRouteLoaderData<T = any>(routeId: string): SerializeFrom<T> | undefined

Returns the `loader` data for a given route by route ID.

Route IDs are created automatically. They are simply the path of the route file relative to the app folder without the extension.

| Route Filename | Route ID

| `app/root.tsx` | `"root"`

| `app/routes/teams.tsx` | `"routes/teams"`

| `app/whatever/teams.$id.tsx` | `"whatever/teams.$id"`

#### Type Parameters

- T = any

#### Parameters

- routeId: string

The ID of the route to return loader data from

#### Returns SerializeFrom<T> | undefined

The data returned from the specified route's `loader` function, or `undefined` if not found

#### Example

```
import { useRouteLoaderData } from "react-router";

function SomeComponent() {
  const { user } = useRouteLoaderData("root");
}

// You can also specify your own route ID's manually in your routes.ts file:
route("/", "containers/app.tsx", { id: "app" })
useRouteLoaderData("app");

@public
```

#### Mode

framework

#### Mode

data

- Defined in packages/react-router/lib/hooks.tsx:1661
