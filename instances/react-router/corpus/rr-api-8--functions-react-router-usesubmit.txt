# Function useSubmit
# джерело: https://api.reactrouter.com/v8/functions/react-router.useSubmit.html
# отримано: 2026-09-25
# версія: 8.4.0

- useSubmit(): SubmitFunction

The imperative version of `<Form>` that lets you submit a form from code instead of a user interaction.

#### Returns SubmitFunction

A function that can be called to submit a Form imperatively.

#### Example

```
import { useSubmit } from "react-router";

function SomeComponent() {
  const submit = useSubmit();
  return (
    <Form onChange={(event) => submit(event.currentTarget)} />
  );
}

@public
```

#### Mode

framework

#### Mode

data

- Defined in packages/react-router/lib/dom/lib.tsx:2552
