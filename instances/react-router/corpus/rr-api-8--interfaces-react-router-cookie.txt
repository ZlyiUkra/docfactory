# Interface Cookie
# джерело: https://api.reactrouter.com/v8/interfaces/react-router.Cookie.html
# отримано: 2026-09-25
# версія: 8.4.0

A HTTP cookie.

A Cookie is a logical container for metadata about a HTTP cookie; its name and options. But it doesn't contain a value. Instead, it has `parse()` and `serialize()` methods that allow a single instance to be reused for parsing/encoding multiple different values.

#### See

https://remix.run/utils/cookies#cookie-api

interface Cookie {
expires?: Date;
isSigned: boolean;
name: string;
parse(
cookieHeader: string | null,
options?: CookieParseOptions,
): Promise<any>;
serialize(value: any, options?: CookieSerializeOptions): Promise<string>;
}

- Defined in packages/react-router/lib/server-runtime/cookies.ts:35

##### Index

### Properties

expires? isSigned name

### Methods

parse serialize

## Properties

### `Optional` `Readonly`expires

expires?: Date

The Date this cookie expires.

Note: This is calculated at access time using `maxAge` when no `expires` option is provided to `createCookie()`.

- Defined in packages/react-router/lib/server-runtime/cookies.ts:52

### `Readonly`isSigned

isSigned: boolean

True if this cookie uses one or more secrets for verification.

- Defined in packages/react-router/lib/server-runtime/cookies.ts:44

### `Readonly`name

name: string

The name of the cookie, used in the `Cookie` and `Set-Cookie` headers.

- Defined in packages/react-router/lib/server-runtime/cookies.ts:39

## Methods

### parse

- parse(cookieHeader: string | null, options?: CookieParseOptions): Promise<any>

Parses a raw `Cookie` header and returns the value of this cookie or `null` if it's not present.

#### Parameters

- cookieHeader: string | null

- `Optional`options: CookieParseOptions

#### Returns Promise<any>

- Defined in packages/react-router/lib/server-runtime/cookies.ts:58

### serialize

- serialize(value: any, options?: CookieSerializeOptions): Promise<string>

Serializes the given value to a string and returns the `Set-Cookie` header.

#### Parameters

- value: any

- `Optional`options: CookieSerializeOptions

#### Returns Promise<string>

- Defined in packages/react-router/lib/server-runtime/cookies.ts:67
