# Interface CookieParseOptions
# джерело: https://api.reactrouter.com/v8/interfaces/react-router.CookieParseOptions.html
# отримано: 2026-09-25
# версія: 8.4.0

Parse options.

interface CookieParseOptions {
allowMultiple?: boolean;
decode?: (str: string) => string | undefined;
filter?(key: string): boolean;
}

- Defined in node_modules/.pnpm/[email protected]/node_modules/cookie-es/dist/index.d.mts:4

##### Index

### Properties

allowMultiple? decode?

### Methods

filter?

## Properties

### `Optional`allowMultiple

allowMultiple?: boolean

When enabled, duplicate cookie names will return an array of values instead of only the first value.

- Defined in node_modules/.pnpm/[email protected]/node_modules/cookie-es/dist/index.d.mts:25

### `Optional`decode

decode?: (str: string) => string | undefined

Specifies a function that will be used to decode a cookie-value. Since the value of a cookie has a limited character set (and must be a simple string), this function can be used to decode a previously-encoded cookie value into a JavaScript string.

The default function is the global `decodeURIComponent`, wrapped in a `try..catch`. If an error is thrown it will return the cookie's original value. If you provide your own encode/decode scheme you must ensure errors are appropriately handled.

#### Default

```
decode
```

- Defined in node_modules/.pnpm/[email protected]/node_modules/cookie-es/dist/index.d.mts:16

## Methods

### `Optional`filter

- filter?(key: string): boolean

Custom function to filter parsing specific keys.

#### Parameters

- key: string

#### Returns boolean

- Defined in node_modules/.pnpm/[email protected]/node_modules/cookie-es/dist/index.d.mts:20
