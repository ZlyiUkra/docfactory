# Interface DataStrategyFunctionArgs<Context>
# джерело: https://api.reactrouter.com/v8/interfaces/react-router.DataStrategyFunctionArgs.html
# отримано: 2026-09-25
# версія: 8.4.0

interface DataStrategyFunctionArgs<Context = DefaultContext> {
context: Context;
fetcherKey: string | null;
matches: DataStrategyMatch[];
params: Params;
pattern: string;
request: Request;
runClientMiddleware: (
cb: DataStrategyFunction<Context>,
) => Promise<Record<string, DataStrategyResult>>;
url: URL;
}

#### Type Parameters

- Context = DefaultContext

#### Hierarchy

- DataFunctionArgs<Context>

- DataStrategyFunctionArgs

- Defined in packages/react-router/lib/router/utils.ts:517

##### Index

### Properties

context fetcherKey matches params pattern request runClientMiddleware url

## Properties

### context

context: Context

This is the context passed in to your server adapter's getLoadContext() function. It's a way to bridge the gap between the adapter's request/response API with your React Router app. It is only applicable if you are using a custom server adapter.

Inherited from DataFunctionArgs.context

- Defined in packages/react-router/lib/router/utils.ts:313

### fetcherKey

fetcherKey: string | null

The key of the fetcher we are calling `dataStrategy` for, otherwise `null` for navigational executions

- Defined in packages/react-router/lib/router/utils.ts:531

### matches

matches: DataStrategyMatch[]

Matches for this route extended with Data strategy APIs

- Defined in packages/react-router/lib/router/utils.ts:523

### params

params: Params

Dynamic route params for the current route.

#### Example

```
// app/routes.ts
route("teams/:teamId", "./team.tsx"),

// app/team.tsx
export function loader({
  params,
}: Route.LoaderArgs) {
  params.teamId;
  //        ^ string
}
```

Inherited from DataFunctionArgs.params

- Defined in packages/react-router/lib/router/utils.ts:307

### pattern

pattern: string

Matched un-interpolated route pattern for the current path (i.e., /blog/:slug). Mostly useful as a identifier to aggregate on for logging/tracing/etc.

Inherited from DataFunctionArgs.pattern

- Defined in packages/react-router/lib/router/utils.ts:292

### request

request: Request

A Fetch Request instance which you can use to read headers (like cookies, and URLSearchParams from the request.

Inherited from DataFunctionArgs.request

- Defined in packages/react-router/lib/router/utils.ts:278

### runClientMiddleware

runClientMiddleware: (
cb: DataStrategyFunction<Context>,
) => Promise<Record<string, DataStrategyResult>>

- Defined in packages/react-router/lib/router/utils.ts:524

### url

url: URL

A URL instance representing the application location being navigated to or fetched.

In Framework mode, this is a normalized URL with React-Router-specific implementation details removed (`.data` suffixes, `index`/`_routes` search params). For the raw incoming URL, use `request.url`.

Inherited from DataFunctionArgs.url

- Defined in packages/react-router/lib/router/utils.ts:287
