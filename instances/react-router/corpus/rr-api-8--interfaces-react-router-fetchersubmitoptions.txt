# Interface FetcherSubmitOptions
# джерело: https://api.reactrouter.com/v8/interfaces/react-router.FetcherSubmitOptions.html
# отримано: 2026-09-25
# версія: 8.4.0

Submit options available to fetchers

interface FetcherSubmitOptions {
action?: string;
defaultShouldRevalidate?: boolean;
encType?: FormEncType;
flushSync?: boolean;
method?: HTMLFormMethod;
preventScrollReset?: boolean;
relative?: RelativeRoutingType;
}

#### Hierarchy (View Summary)

- SharedSubmitOptions

- FetcherSubmitOptions

- SubmitOptions

- Defined in packages/react-router/lib/dom/dom.ts:213

##### Index

### Properties

action? defaultShouldRevalidate? encType? flushSync? method? preventScrollReset? relative?

## Properties

### `Optional`action

action?: string

The action URL path used to submit the form. Overrides `<form action>`. Defaults to the path of the current route.

Inherited from SharedSubmitOptions.action

- Defined in packages/react-router/lib/dom/dom.ts:170

### `Optional`defaultShouldRevalidate

defaultShouldRevalidate?: boolean

Specify the default revalidation behavior after this submission

If no `shouldRevalidate` functions are present on the active routes, then this value will be used directly. Otherwise it will be passed into `shouldRevalidate` so the route can make the final determination on revalidation. This can be useful when updating search params and you don't want to trigger a revalidation.

By default (when not specified), loaders will revalidate according to the routers standard revalidation behavior.

Inherited from SharedSubmitOptions.defaultShouldRevalidate

- Defined in packages/react-router/lib/dom/dom.ts:207

### `Optional`encType

encType?: FormEncType

The encoding used to submit the form. Overrides `<form encType>`. Defaults to "application/x-www-form-urlencoded".

Inherited from SharedSubmitOptions.encType

- Defined in packages/react-router/lib/dom/dom.ts:176

### `Optional`flushSync

flushSync?: boolean

Enable flushSync for this submission's state updates

Inherited from SharedSubmitOptions.flushSync

- Defined in packages/react-router/lib/dom/dom.ts:194

### `Optional`method

method?: HTMLFormMethod

The HTTP method used to submit the form. Overrides `<form method>`. Defaults to "GET".

Inherited from SharedSubmitOptions.method

- Defined in packages/react-router/lib/dom/dom.ts:164

### `Optional`preventScrollReset

preventScrollReset?: boolean

In browser-based environments, prevent resetting scroll after this navigation when using the component

Inherited from SharedSubmitOptions.preventScrollReset

- Defined in packages/react-router/lib/dom/dom.ts:189

### `Optional`relative

relative?: RelativeRoutingType

Determines whether the form action is relative to the route hierarchy or the pathname. Use this if you want to opt out of navigating the route hierarchy and want to instead route based on /-delimited URL segments

Inherited from SharedSubmitOptions.relative

- Defined in packages/react-router/lib/dom/dom.ts:183
