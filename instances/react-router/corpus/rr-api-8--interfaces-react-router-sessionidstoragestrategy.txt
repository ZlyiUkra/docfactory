# Interface SessionIdStorageStrategy<Data, FlashData>
# джерело: https://api.reactrouter.com/v8/interfaces/react-router.SessionIdStorageStrategy.html
# отримано: 2026-09-25
# версія: 8.4.0

SessionIdStorageStrategy is designed to allow anyone to easily build their own SessionStorage using `createSessionStorage(strategy)`.

This strategy describes a common scenario where the session id is stored in a cookie but the actual session data is stored elsewhere, usually in a database or on disk. A set of create, read, update, and delete operations are provided for managing the session data.

interface SessionIdStorageStrategy<Data = SessionData, FlashData = Data> {
cookie?:
| Cookie
| CookieParseOptions & CookieStringifyOptions & Omit<
SetCookie,
"value"
| "name",
> & CookieSignatureOptions & { name?: string };
createData: (
data: FlashSessionData<Data, FlashData>,
expires?: Date,
) => Promise<string>;
deleteData: (id: string) => Promise<void>;
readData: (
id: string,
) => Promise<
| Partial<
Data & {
[Key in string
| number
| symbol as `__flash_${Key & string}__`]: FlashData[Key]
},
>
| null,
>;
updateData: (
id: string,
data: FlashSessionData<Data, FlashData>,
expires?: Date,
) => Promise<void>;
}

#### Type Parameters

- Data = SessionData

- FlashData = Data

- Defined in packages/react-router/lib/server-runtime/sessions.ts:232

##### Index

### Properties

cookie? createData deleteData readData updateData

## Properties

### `Optional`cookie

cookie?:
| Cookie
| CookieParseOptions & CookieStringifyOptions & Omit<
SetCookie,
"value"
| "name",
> & CookieSignatureOptions & { name?: string }

The Cookie used to store the session id, or options used to automatically create one.

- Defined in packages/react-router/lib/server-runtime/sessions.ts:240

### createData

createData: (
data: FlashSessionData<Data, FlashData>,
expires?: Date,
) => Promise<string>

Creates a new record with the given data and returns the session id.

- Defined in packages/react-router/lib/server-runtime/sessions.ts:245

### deleteData

deleteData: (id: string) => Promise<void>

Deletes data for a given session id from the data store.

- Defined in packages/react-router/lib/server-runtime/sessions.ts:267

### readData

readData: (
id: string,
) => Promise<
| Partial<
Data & {
[Key in string
| number
| symbol as `__flash_${Key & string}__`]: FlashData[Key]
},
>
| null,
>

Returns data for a given session id, or `null` if there isn't any.

- Defined in packages/react-router/lib/server-runtime/sessions.ts:253

### updateData

updateData: (
id: string,
data: FlashSessionData<Data, FlashData>,
expires?: Date,
) => Promise<void>

Updates data for the given session id.

- Defined in packages/react-router/lib/server-runtime/sessions.ts:258
