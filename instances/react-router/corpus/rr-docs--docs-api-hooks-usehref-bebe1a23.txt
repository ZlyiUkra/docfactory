# useHref
# джерело: https://github.com/remix-run/react-router/blob/react-router@8.2.0/docs/api/hooks/useHref.md
# отримано: 2026-09-25
# версія: 8.2.0, 8.1.0, 8.0.1, 8.0.0, experimental

## useHref

[MODES: framework, data, declarative]

## Summary

Reference Documentation ↗

Resolves a URL against the current `Location`.

```tsx
import { useHref } from "react-router";

function SomeComponent() {
  let href = useHref("some/where");
  // "/resolved/some/where"
}
```

## Signature

```tsx
function useHref(
  to: To,
  {
    relative,
  }: {
    relative?: RelativeRoutingType;
  } = ,
): string {}
```

## Params

### to

The path to resolve

### options.relative

Defaults to `"route"` so routing is relative to the route tree.
Set to `"path"` to make relative routing operate against path segments.

## Returns

The resolved href string
