# useNavigate
# джерело: https://github.com/remix-run/react-router/blob/react-router@7.5.2/docs/api/hooks/useNavigate.md
# отримано: 2026-09-25
# версія: 7.5.2, 7.5.1, experimental

## useNavigate

[MODES: framework, data, declarative]

## Summary

Reference Documentation ↗

Returns a function that lets you navigate programmatically in the browser in response to user interactions or effects.

```tsx
import { useNavigate } from "react-router";

function SomeComponent() {
  let navigate = useNavigate();
  return (
    <button
      onClick={() => {
        navigate(-1);
      }}
    />
  );
}
```

It's often better to use redirect in ActionFunction and LoaderFunction than this hook.

## Signature

```tsx
useNavigate(): NavigateFunction
```
