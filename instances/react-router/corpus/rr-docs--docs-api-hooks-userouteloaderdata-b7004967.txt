# useRouteLoaderData
# джерело: https://github.com/remix-run/react-router/blob/react-router@7.13.0/docs/api/hooks/useRouteLoaderData.md
# отримано: 2026-09-25
# версія: 7.13.0, 7.12.0, 7.11.0, 7.10.1, 7.10.0, 7.9.6, 7.9.5, 7.9.4, 7.9.3, 7.9.2, 7.9.1, 7.9.0, 7.8.2, 7.8.1, 7.8.0, 7.7.1, nightly, experimental

## useRouteLoaderData

[MODES: framework, data]

## Summary

Reference Documentation ↗

Returns the `loader` data for a
given route by route ID.

Route IDs are created automatically. They are simply the path of the route file
relative to the app folder without the extension.

| Route Filename               | Route ID               |
| ---------------------------- | ---------------------- |
| `app/root.tsx`               | `"root"`               |
| `app/routes/teams.tsx`       | `"routes/teams"`       |
| `app/whatever/teams.$id.tsx` | `"whatever/teams.$id"` |

```tsx
import { useRouteLoaderData } from "react-router";

function SomeComponent() {
  const { user } = useRouteLoaderData("root");
}

// You can also specify your own route ID's manually in your routes.ts file:
route("/", "containers/app.tsx", { id: "app" })
useRouteLoaderData("app");
```

## Signature

```tsx
function useRouteLoaderData<T = any>(
  routeId: string,
): SerializeFrom<T> | undefined
```

## Params

### routeId

The ID of the route to return loader data from

## Returns

The data returned from the specified route's `loader`
function, or `undefined` if not found
