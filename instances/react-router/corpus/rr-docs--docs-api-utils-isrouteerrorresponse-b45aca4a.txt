# isRouteErrorResponse
# джерело: https://github.com/remix-run/react-router/blob/react-router@7.9.6/docs/api/utils/isRouteErrorResponse.md
# отримано: 2026-09-25
# версія: 7.9.6, 7.9.5, 7.9.4, 7.9.3, 7.9.2, 7.9.1, 7.9.0, 7.8.2, 7.8.1, 7.8.0, experimental

## isRouteErrorResponse

[MODES: framework, data]

## Summary

Reference Documentation ↗

Check if the given error is an `ErrorResponse` generated from a 4xx/5xx
`Response`
thrown from an `action`/`loader`

```tsx
import { isRouteErrorResponse } from "react-router";

export function ErrorBoundary({ error }: Route.ErrorBoundaryProps) {
  if (isRouteErrorResponse(error)) {
    return (
      <>
        <p>Error: `${error.status}: ${error.statusText}`</p>
        <p>{error.data}</p>
      </>
    );
  }

  return (
    <p>Error: {error instanceof Error ? error.message : "Unknown Error"}</p>
  );
}
```

## Signature

```tsx
function isRouteErrorResponse(error: any): error is ErrorResponse
```

## Params

### error

The error to check.

## Returns

`true` if the error is an `ErrorResponse`, `false` otherwise.
