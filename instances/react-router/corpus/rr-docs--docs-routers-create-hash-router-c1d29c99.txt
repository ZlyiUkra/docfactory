# createHashRouter
# джерело: https://github.com/remix-run/react-router/blob/react-router@6.4.4/docs/routers/create-hash-router.md
# отримано: 2026-09-25
# версія: 6.4.4, 6.4.4-pre.2, 6.4.4-pre.1, 6.4.4-pre.0, 6.4.3, 6.4.3-pre.2, 6.4.3-pre.1, 6.4.3-pre.0, 6.4.2, 6.4.2-pre.1, 6.4.2-pre.0, 6.4.1, 6.4.1-pre.0, 6.4.0

## `createHashRouter`

This router is useful if you are unable to configure your web server to direct all traffic to your React Router application. Instead of using normal URLs, it will use the hash (#) portion of the URL to manage the "application URL".

Using hash URLs is not recommended.

Other than that, it is functionally the same as `createBrowserRouter`.

```tsx
import * as React from "react";
import * as ReactDOM from "react-dom";
import {
  createHashRouter,
  RouterProvider,
} from "react-router-dom";

import Root, { rootLoader } from "./routes/root";
import Team, { teamLoader } from "./routes/team";

const router = createHashRouter([
  {
    path: "/",
    element: <Root />,
    loader: rootLoader,
    children: [
      {
        path: "team",
        element: <Team />,
        loader: teamLoader,
      },
    ],
  },
]);

ReactDOM.createRoot(document.getElementById("root")).render(
  <RouterProvider router={router} />
);
```

[createbrowserrouter]: ./create-browser-router
