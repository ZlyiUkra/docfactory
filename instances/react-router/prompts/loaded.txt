Loaded right now: {excerpts} excerpts from {documents} documents of the official React
Router documentation -- the docs kept in the repository on every one of its 820 tags
(every release from 0.0 to the current line, every pre-release, beta and release
candidate, and the nightly and experimental builds), each distinct text once with all
the versions it belongs to; the TypeDoc API reference of api.reactrouter.com for 8.4.0
and 7.18.4; the release notes in CHANGELOG.md from v6.0.0 on, the changelog of the
react-router package, and the GitHub release notes of every release. Release lines
present: {versions}. React itself, Remix before React Router 7, Next.js and other
routers are not here; if something about React Router is not found in these documents,
it is unlikely to be anywhere in its official documentation.
