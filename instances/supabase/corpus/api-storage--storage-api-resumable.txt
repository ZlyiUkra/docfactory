# Storage API: resumable
# джерело: https://supabase.com/docs/reference/self-hosting-storage/introduction
# отримано: 2026-09-24

Resumable Upload end-points

## POST /upload/resumable/

Handle POST request for TUS Resumable uploads

Security: bearerAuth

Responses:

- `200`: Default Response

## OPTIONS /upload/resumable/

Handle OPTIONS request for TUS Resumable uploads

Handle OPTIONS request for TUS Resumable uploads

Responses:

- `200`: Default Response

## POST /upload/resumable/{*}

Handle POST request for TUS Resumable uploads

Security: bearerAuth

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## PUT /upload/resumable/{*}

Handle PUT request for TUS Resumable uploads

Security: bearerAuth

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## PATCH /upload/resumable/{*}

Handle PATCH request for TUS Resumable uploads

Security: bearerAuth

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## DELETE /upload/resumable/{*}

Handle DELETE request for TUS Resumable uploads

Security: bearerAuth

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## HEAD /upload/resumable/{*}

Handle HEAD request for TUS Resumable uploads

Security: bearerAuth

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## OPTIONS /upload/resumable/{*}

Handle OPTIONS request for TUS Resumable uploads

Handle OPTIONS request for TUS Resumable uploads

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## POST /upload/resumable/sign/

Handle POST request for TUS Resumable uploads

Responses:

- `200`: Default Response

## OPTIONS /upload/resumable/sign/

Handle OPTIONS request for TUS Resumable uploads

Handle OPTIONS request for TUS Resumable uploads

Responses:

- `200`: Default Response

## POST /upload/resumable/sign/{*}

Handle POST request for TUS Resumable uploads

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## PUT /upload/resumable/sign/{*}

Handle PUT request for TUS Resumable uploads

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## PATCH /upload/resumable/sign/{*}

Handle PATCH request for TUS Resumable uploads

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## DELETE /upload/resumable/sign/{*}

Handle DELETE request for TUS Resumable uploads

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## HEAD /upload/resumable/sign/{*}

Handle HEAD request for TUS Resumable uploads

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response

## OPTIONS /upload/resumable/sign/{*}

Handle OPTIONS request for TUS Resumable uploads

Handle OPTIONS request for TUS Resumable uploads

Parameters:

- `*` (path, required) `string`

Responses:

- `200`: Default Response
