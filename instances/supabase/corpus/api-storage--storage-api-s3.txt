# Storage API: s3
# джерело: https://supabase.com/docs/reference/self-hosting-storage/introduction
# отримано: 2026-09-24

S3 end-points

## GET /s3/{Bucket}/{*}

Parameters:

- `Bucket` (path, required) `string`
- `*` (path, required) `string`

Responses:

- `200`: Default Response

## POST /s3/{Bucket}/{*}

Parameters:

- `Bucket` (path, required) `string`
- `*` (path, required) `string`

Responses:

- `200`: Default Response

## PUT /s3/{Bucket}/{*}

Parameters:

- `Bucket` (path, required) `string`
- `*` (path, required) `string`

Responses:

- `200`: Default Response

## DELETE /s3/{Bucket}/{*}

Parameters:

- `Bucket` (path, required) `string`
- `*` (path, required) `string`

Responses:

- `200`: Default Response

## HEAD /s3/{Bucket}/{*}

Parameters:

- `Bucket` (path, required) `string`
- `*` (path, required) `string`

Responses:

- `200`: Default Response

## GET /s3/{Bucket}

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## POST /s3/{Bucket}

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## PUT /s3/{Bucket}

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## DELETE /s3/{Bucket}

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## HEAD /s3/{Bucket}

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## GET /s3/{Bucket}/

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## POST /s3/{Bucket}/

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## PUT /s3/{Bucket}/

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## DELETE /s3/{Bucket}/

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## HEAD /s3/{Bucket}/

Parameters:

- `Bucket` (path, required) `string`

Responses:

- `200`: Default Response

## GET /s3/

Responses:

- `200`: Default Response
