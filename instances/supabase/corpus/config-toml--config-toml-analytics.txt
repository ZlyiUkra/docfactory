# config.toml: Analytics
# джерело: https://supabase.com/docs/guides/local-development/cli/config
# отримано: 2026-09-24

Analytics settings.

## analytics.enabled

- Setting: key of supabase/config.toml (local project and supabase config push)
- In words: analytics enabled
- Required: no
- Default: `false`

Enable the local Logflare service.

See also:

- Self-hosted Logflare Configuration: https://supabase.com/docs/reference/self-hosting-analytics/list-endpoints#getting-started

## analytics.port

- Setting: key of supabase/config.toml (local project and supabase config push)
- In words: analytics port
- Required: no
- Default: `54327`

Port to the local Logflare service.

## analytics.vector_port

- Setting: key of supabase/config.toml (local project and supabase config push)
- In words: analytics vector port
- Required: no
- Default: `54328`

Port to the local syslog ingest service.

## analytics.backend

- Setting: key of supabase/config.toml (local project and supabase config push)
- In words: analytics backend
- Required: no
- Default: `"postgres"`

Configure one of the supported backends:

- `postgres`
- `bigquery`

See also:

- Self-hosted Logflare Configuration: https://supabase.com/docs/reference/self-hosting-analytics/list-endpoints#getting-started
