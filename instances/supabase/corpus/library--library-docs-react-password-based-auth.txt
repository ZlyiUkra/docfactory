# Password-based Authentication
# джерело: https://supabase.com/library/docs/react/password-based-auth
# отримано: 2026-09-24

Password-based authentication block for React Single Page Applications

## Installation

Install this block:

```bash
npx shadcn@latest add @supabase/password-based-auth-react
```

## Folder structure

This block includes the Supabase client. If you already have one installed, you can skip overwriting it.

Includes first-party dependencies: safe-next-path.

- `components/`
  - `forgot-password-form.tsx`
  - `login-form.tsx`
  - `sign-up-form.tsx`
  - `update-password-form.tsx`
- `lib/`
  - `safe-next-path.ts`
  - `supabase/`
    - `client.ts`

External registry dependencies: button, card, input, label.

Full source: https://supabase.com/library/r/password-based-auth-react.json
Full source: https://supabase.com/library/r/safe-next-path.json

## Usage

Once you install the block in your React project, you'll get all the necessary pages and components to set up a password-based authentication flow.

### Getting started

After installing the block, you'll have the following environment variables in your `.env.local` file:

```env
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
```

- If you're using supabase.com, you can find these values in the Connect modal under App Frameworks or in your project's API settings.

- If you're using a local instance of Supabase, you can find these values by running `supabase start` or `supabase status` (if you already have it running).

### Setting up routes and redirect URLs

1. Set the site URL in the URL Configuration settings in the Supabase Dashboard.

2. Set up the route users will visit to reset or update their password. Go to the URL Configuration settings and add the `forgot-password` route to the list of Redirect URLs. It should look something like: `http://example.com/auth/forgot-password`.

3. Update the redirect paths in `login.tsx` and `update-password.tsx` components to point to the logged-in routes in your app. Our examples use `/protected`, but you can set this to whatever fits your app.

4. Add the following code in the authenticated route to redirect to login if the user is unauthenticated.

```ts
import { useEffect } from "react"
import { createClient } from "@supabase/supabase-js"

export default function AuthenticatedRoute() {
  useEffect(() => {
    const checkAuth = async () => {
      const client = createClient()
      const { error } = await client.auth.getUser()

      if (error) {
        location.href = "/login"
      }
    }
    checkAuth()
  }, [])

  return <div>Authenticated page</div>;
}
```

## Further reading

- Password-based authentication (implicit flow)
- Authentication error codes
- Email templates
- Email templates for local development
- Custom SMTP
