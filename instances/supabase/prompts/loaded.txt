Loaded right now: {excerpts} excerpts from {documents} documents of the official Supabase
documentation -- every guide page and troubleshooting article of supabase.com/docs, the
features catalog, the current reference of the JavaScript, Dart, Swift, Kotlin, Python
and C# client libraries and the older majors the site still shows (marked legacy), the
Server SDK and its middleware, the CLI and the Management API, the REST APIs of the
Auth, Storage and Analytics servers, the configuration references of
`supabase/config.toml` and of the self-hosted Auth, Storage, Realtime, Analytics and
Edge Functions servers, Supabase Library, the changelog, the blog and customer stories.
There is no version filter: this is the current text of a continuously deployed
platform. Postgres, PostgREST, Deno and the frameworks and hosts Supabase integrates
with are not here; only Supabase's own side of each integration is.
