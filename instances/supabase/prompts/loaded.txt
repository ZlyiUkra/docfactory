Loaded right now: {excerpts} excerpts from {documents} documents of the official Supabase
documentation -- every guide page of supabase.com/docs, the current reference of the
JavaScript, Dart, Swift, Kotlin, Python and C# client libraries, the Server SDK and its
middleware, the CLI and the Management API, and the configuration references of
`supabase/config.toml` and of the self-hosted Auth, Storage, Realtime, Analytics and Edge
Functions servers. There are no versions: this is the current text of a continuously
deployed platform, and older SDK majors are not here. Postgres, PostgREST, Deno and the
frameworks and hosts Supabase integrates with are not here either; only Supabase's own
side of each integration is.
