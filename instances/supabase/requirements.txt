# supabase · примірник фабрики: документація Supabase — гайди, референс SDK, CLI й API,
# довідники налаштувань. Лише те, що потрібно.
#
# Python 3.10+. Склад той самий, що в astro, плюс PyYAML: довідники налаштувань
# (config.toml, змінні self-hosting) існують лише як YAML-специфікації, і читач
# config-spec розбирає їх цією бібліотекою. Решта читачів — на стандартній бібліотеці.

anthropic>=0.40,<1
python-dotenv>=1.0.0

# MCP-сервер і клієнт. У SDK 2.0 клас FastMCP перейменували на MCPServer —
# код підтримує обидві версії.
mcp[cli]>=2.1,<3

# Пошук за змістом (необовʼязковий). ONNX замість torch: імпортується за секунди,
# без тисяч дрібних файлів. Без нього працює пошук лише по словах.
fastembed>=0.8,<1

# Читач config-spec. Лише safe_load, без розширень на C — чистий Python годиться.
pyyaml>=6,<7
