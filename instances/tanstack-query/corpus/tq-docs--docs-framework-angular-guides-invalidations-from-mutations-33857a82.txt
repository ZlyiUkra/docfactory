# Invalidations from Mutations
# джерело: https://github.com/TanStack/query/blob/v5.62.12/docs/framework/angular/guides/invalidations-from-mutations.md
# отримано: 2026-09-26
# версія: 5.62.12, 5.62.11, 5.62.10, 5.62.9, 5.62.8, 5.62.7, 5.62.6, 5.62.5, 5.62.4, 5.62.3, 5.62.2, 5.62.1, 5.62.0, 5.61.6, 5.61.5, 5.61.4, 5.61.3, 5.61.2, 5.61.1, 5.61.0, 5.60.7

[//]: # 'Example'

```ts
class TodoItemComponent {
  mutation = injectMutation(() => ({
    mutationFn: postTodo,
  }))
}
```

[//]: # 'Example'
[//]: # 'Example2'

```ts
import {
  injectMutation,
  QueryClient,
} from '@tanstack/angular-query-experimental'

export class TodosComponent {
  queryClient = inject(QueryClient)

  // When this mutation succeeds, invalidate any queries with the `todos` or `reminders` query key
  mutation = injectMutation(() => ({
    mutationFn: addTodo,
    onSuccess: () => {
      this.queryClient.invalidateQueries({ queryKey: ['todos'] })
      this.queryClient.invalidateQueries({ queryKey: ['reminders'] })

      // OR use the queryClient that is injected into the component
      // this.queryClient.invalidateQueries({ queryKey: ['todos'] })
    },
  }))
}
```

[//]: # 'Example2'

You can wire up your invalidations to happen using any of the callbacks available in the `injectMutation` function
