# TimeoutCallback
# джерело: https://github.com/TanStack/query/blob/@tanstack%2Fquery-core@5.103.3/docs/framework/lit/reference/type-aliases/TimeoutCallback.md
# отримано: 2026-09-26
# версія: 5.103.3, 5.103.2

```ts
type TimeoutCallback = (_: void) => void;
```

Defined in: packages/query-core/src/timeoutManager.ts:9

TimeoutManager does not support passing arguments to the callback.

`(_: void)` is the argument type inferred by TypeScript's default typings for
`setTimeout(cb, number)`.
If we don't accept a single void argument, then
`new Promise(resolve => timeoutManager.setTimeout(resolve, N))` is a type error.

## Parameters

### \_

`void`

## Returns

`void`
