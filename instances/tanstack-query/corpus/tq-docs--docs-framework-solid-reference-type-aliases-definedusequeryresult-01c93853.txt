# DefinedUseQueryResult
# джерело: https://github.com/TanStack/query/blob/@tanstack%2Fquery-core@5.103.1/docs/framework/solid/reference/type-aliases/DefinedUseQueryResult.md
# отримано: 2026-09-26
# версія: 5.103.1, 5.103.0

```ts
type DefinedUseQueryResult<TData, TError> = DefinedUseBaseQueryResult<TData, TError>;
```

Defined in: packages/solid-query/src/types.ts:142

The object `useQuery` returns when `initialData` guarantees `data` is never `undefined`.

## Type Parameters

### TData

`TData` = `unknown`

The type `data` ends up as, after `select` runs (if set).

### TError

`TError` = `DefaultError`

The type of errors this query may hold.
