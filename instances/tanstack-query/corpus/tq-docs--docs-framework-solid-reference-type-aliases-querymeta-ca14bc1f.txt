# QueryMeta
# джерело: https://github.com/TanStack/query/blob/@tanstack%2Fquery-core@5.103.3/docs/framework/solid/reference/type-aliases/QueryMeta.md
# отримано: 2026-09-26
# версія: 5.103.3

```ts
type QueryMeta = Register extends object ? TQueryMeta extends Record<string, unknown> ? TQueryMeta : Record<string, unknown> : Record<string, unknown>;
```

Defined in: packages/query-core/src/types.ts:258

The type of the `meta` object that can be attached to a query and read back from `queryFn`, callbacks and
cache-level handlers. Defaults to `Record<string, unknown>`; declare `queryMeta` on Register to narrow it.
