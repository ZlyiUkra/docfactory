# QueriesObserver
# джерело: https://github.com/TanStack/query/blob/v4.0.4/docs/reference/QueriesObserver.md
# отримано: 2026-09-26
# версія: 4.0.4, 4.0.3, 4.0.2, 4.0.1, 4.0.0

## `QueriesObserver`

The `QueriesObserver` can be used to observe multiple queries.

```js
const observer = new QueriesObserver(queryClient, [
  { queryKey: ['post', 1], queryFn: fetchPost },
  { queryKey: ['post', 2], queryFn: fetchPost },
])

const unsubscribe = observer.subscribe(result => {
  console.log(result)
  unsubscribe()
})
```

**Options**

The options for the `QueriesObserver` are exactly the same as those of `useQueries`.
