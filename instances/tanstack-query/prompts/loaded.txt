Loaded right now: {excerpts} excerpts from {documents} documents of the official TanStack
Query documentation -- the docs kept in the repository on every one of its release tags
(from 0.0.3 to the current 5.x, with every alpha, beta and release candidate), each distinct
text once with all the versions it belongs to, including the API reference of every
framework adapter; the example apps of every major line; the changelogs of all
@tanstack/*-query packages; the GitHub release notes; and the TanStack blog posts about
Query. Release lines present: {versions}. TanStack Router, Start, Table, Form and the
frameworks themselves are not here; if something about TanStack Query is not found in these
documents, it is unlikely to be anywhere in its official documentation.
