# tanstack-query · примірник фабрики: документація TanStack Query усіх версій. Лише те, що потрібно.
#
# Python 3.10+. Склад той самий, що в react: жодне джерело не видається PDF-ом, а
# читачі репозиторію й довідника API (markdown, HTML, JSON) — на стандартній
# бібліотеці. Перевірено 25.09.2026 на Python 3.14.6 з тими самими версіями, що в
# instances/react/requirements.txt; верхні межі — ті самі й з тієї ж причини.

anthropic>=0.40,<1
python-dotenv>=1.0.0

# MCP-сервер і клієнт. У SDK 2.0 клас FastMCP перейменували на MCPServer —
# код підтримує обидві версії.
mcp[cli]>=2.1,<3

# Пошук за змістом (необовʼязковий). ONNX замість torch: імпортується за секунди,
# без тисяч дрібних файлів. Без нього працює пошук лише по словах.
fastembed>=0.8,<1
